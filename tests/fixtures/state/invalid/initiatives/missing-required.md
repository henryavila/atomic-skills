---
schemaVersion: '0.1'
slug: invalid-fixture
title: 'Missing required fields'
status: active
---

# Body kept intentionally minimal — this fixture is for negative testing.

It omits `goal`, `branch`, `started`, `lastUpdated`, `nextAction`, `exitGates`, `stack`, `tasks`, `parked`, `emerged`.
