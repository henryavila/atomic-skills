| # | Severity | File:line | Claim | Impact | Recommendation | Confidence |
|---:|---|---|---|---|---|---|
| 1 | critical | `src/plan-end-review.js:39` | `planEndReviewOk` counts any `{status: "succeeded"}` leg as a valid family-different external review because `provider` is ignored and missing `familyDifferent` defaults to pass. | A malformed, same-family, or host-local receipt can satisfy the hard finalize/archive review gate without a real external family-different review. | Require an explicit trusted external leg shape, e.g. known provider plus `familyDifferent === true`, and add negative tests for missing provider and same-family succeeded legs. | high |
| 2 | major | `src/plan-end-review.js:90` | `userValidationOk` claims to require an ISO timestamp but only checks for any non-empty string. | `userValidatedAt: "ok"` or `"not a date"` passes automate validation, so downstream finalize/archive can treat invalid operator validation as real. | Parse and validate ISO date strings, ideally requiring round-trip-valid timestamps, and add negative tests for arbitrary strings and invalid dates. | high |
| 3 | major | `src/implement-mode.js:82` | `parseImplementMode(['--mode='])` is silently ignored because the regex requires one or more chars after `=`, so the explicit but empty mode falls through to `default`. | A mistyped explicit mode does not fail closed; it runs the default Mode 1 path instead of refusing with a clear error. | Match `^--mode=(.*)$`, reject empty/blank values, and add tests for `--mode=` and `--mode=   `. | high |
| 4 | major | `src/implement-mode.js:19` | The parser accepts `--mode=2`, `--mode=mode2`, and `--mode=codex`, but this change does not implement or dispatch a Mode 2 lane; `isAutomateActive` just returns false for them. | An explicit request for the reserved Mode 2 surface can be accepted but then proceed as non-automate/default behavior, creating a dangerous precedence/routing ambiguity. | Reject unsupported reserved modes until the real Mode 2 path consumes them, or return a distinct mode that callers must handle before continuing. | high |
| 5 | major | `src/implement-mode.js:154` | Explicit non-automate CLI mode cannot override an `executionMode: automate` stamp because `isAutomateActive` lacks a `modeExplicit` signal and activates on the stamp after seeing `cliMode: "1"`. | A user can pass `--mode=1` and still remain in automate mode if the plan is stamped, contradicting the documented “`--mode=1` stays Mode 1” behavior. | Return `modeExplicit` from `parseImplementMode` and make explicit non-automate CLI mode override the stamp, or remove/document that only `--clear-execution-mode` disables a stamp and test that contract directly. | medium |