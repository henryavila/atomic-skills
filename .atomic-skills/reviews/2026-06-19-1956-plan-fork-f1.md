# Review — plan-fork F1 (verbo fork-plan + degrau 7.5)

- **Date:** 2026-06-19 19:56
- **Scope/ref:** `d11705c..HEAD` (phase F1 diff)
- **Mode:** local (same-model sealed envelope; clean-context agent)
- **Files reviewed:** 4 (`skills/core/project.md`, `skills/shared/project-assets/project-emergence.md`, F1 initiative state, `focus.json`)
- **Passes:** 2
- **Counts (local):** blocker 0, critical 2, major 2, minor 1
- **Verdict:** findings_exist → all applied → converged (5 found → 5 fixed → 0 new on re-read)
- **Fixes commit:** `4e23baf` (`fix(plan-fork): F1 review — corrige 5 findings no procedure fork-plan`)

## Findings & resolution

| # | Severity | Finding | Resolution |
|---|---|---|---|
| 1 | critical | `--mode parallel` rejection lived only in prose (Mode semantics), not in the numbered Steps — `parallel` is in-enum so the arg-parse check did not catch it; an executor walking Steps top-to-bottom reached the sidecar write with `parallel`. | Added numbered **step 2** that rejects `parallel` before any gate/write; arg-parse note clarifies `parallel` parses valid but is runtime-rejected in F1. |
| 2 | critical | Parent pause ran in step 7 (after the `new plan` handoff, step 6). At handoff the parent was still `active`, so `new plan` Stage 6 single-active pre-flight (R-FOCUS-01) would fire its multi-active resolution prompt, contradicting the pause. | Moved parent+anchor pause to **step 6, before the step-7 handoff**, so the pre-flight sees one active plan (the child). |
| 3 | major | "reuses cascade-pause" demotes the **active** phase by status, not the named `--from` anchor; `--from` was validated only for existence, so a non-active anchor would pause the wrong phase. | Constrained `--from` to equal `currentPhase` for `--mode pause`; pause now targets the **named** `--from` phase explicitly. |
| 4 | major | `setSpawnedFrom` (step 5) `mkdirSync`+wrote `links.json` before the child plan existed (step 6 handoff). On a DESIGN-gate block / user cancel, an orphan `links.json` + dangling parent edge persisted with no rollback. | Moved the bidirectional write to **step 8 (after child materialization)**; added pause-rollback on step-7 handoff abort. |
| 5 | minor | Cycle-check input omitted a first-time-forking parent (no sidecar → `getSpawnedPlans` `{}`), so `buildAdjacency` might not materialize the parent node. | Step 4 now **always includes the parent** in the plan list (`spawnedPlans: {}` if absent). |

## Checklist (from the sealed briefing)

- Logic bugs: findings #1/#2/#3 (resolved)
- Race conditions: ok (parallel cross-worktree correctly deferred to F2; pause is single-tree)
- Error handling: findings #1/#4 (resolved)
- Schema/migrations: ok (`links.schema.json` matches the edge shape; inline migration deferred to F5)
- API contracts: ok (`setSpawnedFrom`/`addSpawnedPlan`/`getSpawnedPlans`/`buildAdjacency`/`wouldCreateCycle` signatures match the doc)
- File references: ok (all referenced paths/symbols resolve)
- Test coverage: noted — F1 phase verifiers are token-presence greps only; none of #1–#4 would be caught by a verifier (the gate quality is editorial, carried by this review, not by the grep)

## Note

The review was on `d11705c..HEAD` (the committed F1 diff). The same-model local pass found 5 substantive procedural-correctness defects — evidence that an editorially-gated phase (weak grep verifiers) genuinely needs the review gate, not just the verifier.
