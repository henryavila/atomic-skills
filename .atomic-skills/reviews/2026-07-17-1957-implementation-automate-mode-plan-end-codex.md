| # | Severity | File:line | Claim | Impact | Recommendation | Confidence |
|---:|---|---|---|---|---|---|
| 1 | Blocker | `src/implement-mode.js:195` | Explicit known non-automate CLI modes return `false` even when plan frontmatter is stamped `executionMode: automate`. WHAT: `--mode=1` can deactivate automate for a stamped plan. WHY: this contradicts durable stamp semantics and creates the silent Mode-1 fallback hole the feature is meant to close. | A stamped automate plan can be run as session-writer without first clearing the stamp or proving the writer lease / merge state is clean, bypassing pure-maestro and review cadence. | Do not let `--mode=1` override an automate stamp. Require explicit `--clear-execution-mode`, clean/missing lease, no mid-merge, durable unstamp, and decision-log entry before Mode 1. | High |
| 2 | Critical | `src/plan-end-review.js:182` | `automatePlanEndGatesOk({})` returns `{ ok: true }` unless `automateActive === true`. WHAT: the combined finalize/archive gate fails open on omitted or misnamed mode input. WHY: callers must pass the exact boolean; even docs use `clearFlag` while the helper expects `clearExecutionMode`. | A caller typo or missing automate computation can archive/finalize an automate-stamped plan without `planEndReviewOk` or `userValidationOk`. | Make the combined gate take `planExecutionMode`/`clearExecutionMode` and compute internally, or fail closed/throw when automate state is not explicit. Add negative call-site tests. | High |
| 3 | Critical | `src/plan-end-review.js:100` | `planEndReviewOk` accepts any succeeded known external leg, ignoring `mode`, `range`, `reviewFile`, and `verifiedAt`. WHAT: a minimal forged frontmatter object can satisfy the gate. WHY: schema also does not require those receipt fields. | Plan-end review can self-certify without a persisted review artifact, reviewed range, timestamp, or external-both receipt identity. | Require `mode: external-both`, non-empty `range`, `reviewFile`, `verifiedAt`, and at least one valid leg or explicit skip reason; have finalize/archive verify artifact/link existence. | High |
| 4 | Critical | `src/claim-report.js:459` | Reachability accepts prefix matches in either direction. WHAT: a claimed short SHA like `a` passes if any reachable commit starts with `a`. WHY: uniqueness/object existence is not proven. | Orchestrator can treat an ambiguous or wrong claim identity as reachable, then re-verify/review/close against the wrong commit range. | Require full object IDs or validate abbreviations via git uniqueness (`rev-parse --verify`) before reachability; remove generic prefix matching from the pure helper. | High |
| 5 | Critical | `src/claim-report.js:291` | Base/head ranges are automatically treated as exclusive and skipped from overlap checks. WHAT: two open task claims can share the same broad `base..head` range and still validate. WHY: exclusivity is asserted by representation, not verified. | Multi-task claim reports can pin the same diff for multiple tasks, corrupting destructive-diff, review range, and `done` attribution. | Reject identical range pairs across open claims at minimum; preferably require per-task exclusive ranges or explicit task-to-commit mapping before review/close. | High |