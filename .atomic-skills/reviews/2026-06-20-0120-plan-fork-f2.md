# Review — plan-fork F2 (protocolo de estado parallel cross-worktree)

- **Date:** 2026-06-20 01:20
- **Scope/ref:** `669cac6..HEAD` (phase F2 diff: spec + src/parallel-state.js + schema + tests)
- **Mode:** both (local sealed-envelope agent → codex cross-model on the same captured diff)
- **DESTRUCTIVE:** false (additive; no files deleted) — `--mode=both` chosen anyway per lesson design-brief-F0 L-001 (concurrency protocol = contract expensive to revert).
- **Fixes commit:** `1f24eb3` (`fix(plan-fork): F2 review (both) — corrige 11 findings`)
- **Reviewer models:** local = same-model sealed agent; codex = gpt-5-codex (blind pass).

## Outcome

11 findings total, all applied. The two passes **converged** on 3 shared issues (strong cross-model confirmation) and codex surfaced **2 disjoint** issues the local same-model pass missed — the exact cross-model value L-001 predicts. Tests 14 → 64.

## Local pass (sealed same-model agent) — 6 findings

| # | Sev | Finding | Fix |
|---|---|---|---|
| 1 | critical | Stale `.links.lock` from a crashed writer deadlocks ALL future writebacks forever (no TTL/pid check). | Stale-lock recovery: pid+ts stamped in the lock; reclaim if the owning pid is dead or the lock is older than `LOCK_TTL_MS` (30s). |
| 2 | major | Zero-delay 50-iter spin loop (~1ms) → false `lock-timeout` under real contention (reproduced with 2 OS processes). | `sleepSync` (Atomics.wait) backoff between attempts. |
| 3 | major | Temp file leaks on `renameSync` failure (finally only cleaned the lock). | Temp tracked; unlinked best-effort in `finally` on any write/rename throw. |
| 4 | major | `.tmp-${pid}` not collision-safe (pid reuse / same process). | Unique suffix `${pid}-${randomBytes(6).hex}`. |
| 5 | minor | `pendingWriteback.args` unconstrained → malformed replay payload validates green, detonates at F3 replay. | Schema `if/then` per-op (`addSpawnedPlan`→{phaseId,childSlug}, `resumeParent`→{phaseId}) + `args` required + negative tests. |
| 6 | minor | `contentToken` throws opaque EISDIR on a directory path. | `statSync().isFile()` guard with a clear error. |

Plus a robustness refinement applied during triage: the lock fd is closed right after the pid/ts stamp (it is the file's existence, not a held fd) so a stamp-write failure cannot leak the fd.

## Codex pass (gpt-5-codex, blind) — 5 findings

verdict: needs_changes · counts: 0 blocker / 0 critical / 5 major.

| codex # | Maps to | Disposition |
|---|---|---|
| F-002 (stale-lock deadlock) | = local #1 | **Cross-confirmed.** Already fixed. |
| F-003 (temp pid-only, no exclusive create, no cleanup) | = local #3 + #4 | **Cross-confirmed.** Already fixed. |
| F-005 (concurrency test is sequential; a remove-the-lock mutation would still pass) | = local test-coverage note | **Cross-confirmed.** Added a deterministic lock-gates test: a live+fresh held lock with `retries: 2` returns `lock-timeout` and writes nothing — removing `openSync(wx)` makes it wrongly succeed → the test fails. Kills the mutation. |
| **F-001 (DISJOINT)** | — | **New — local pass missed it.** `atomicWriteback` returned a conflict WITHOUT recording the durable `pendingWriteback` (left to the caller; a caller crash loses recovery). Fix: `writebackOrDefer` — single conflict-handling API that records `pendingWriteback` BEFORE returning a conflict, and clears it on success. Test asserts the child `links.json` carries the marker on conflict. |
| **F-004 (DISJOINT)** | sharper than local #6 | **New — local pass missed it.** `readToken: null` against an absent canonical file passed the `null===null` CAS → silently created state in the wrong place. Fix: `allowCreate=false` default → absent target returns `{conflict, reason: 'canonical-absent'}`; `allowCreate: true` only for legitimately-fresh targets. Tests for both. |

## Checklist (consolidated)

- Logic bugs / Race conditions / Error handling: findings #1–#4 + F-001/F-004 (resolved)
- Schema: finding #5 (resolved — per-op args constraint)
- API contracts (code vs spec `docs/design/plan-fork-parallel-state.md`): both passes confirmed the code matches the spec; F-001 tightened the conflict→recovery wiring the spec §6 requires.
- Test coverage: F-005 (resolved — lock-gates mutation test); 14 → 64 tests, G9 mutation-kill on the conflict predicate recorded in T-002 evidence.

## Note (the cross-model value, for the lessons loop)

The local same-model pass found the 3 lock/temp issues but **missed F-001 (conflict-without-recovery-record) and F-004 (null-token absent-file write)** — both real architectural gaps. The codex cross-model pass caught them blind. This is the second time on this plan (cf. F1 had no codex pass; F0's lesson L-001 noted codex catching contract-rigor issues the local pass missed). Confirms design-brief-F0 L-001: for a one-way-door contract, run `--mode=both`.
