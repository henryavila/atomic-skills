---
date: 2026-06-17T16:51:02Z
topic: wlf-f2-teardown-squash-safe
artifact: 8796c6e..HEAD (scripts/worktree-teardown.js + tests/worktree-teardown.test.js)
skill: review-code
reviewer: gpt-5-codex
codex_version: codex-cli 0.139.0
final_verdict: approve
counts_final: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
counts_blind: {blocker: 0, critical: 0, major: 1, minor: 1, nit: 0}
framing_delta: {dropped: 2, maintained: 0, emerged: 0}
schema_version: "1.0"
---

# Cross-Model Review — wlf-f2-teardown-squash-safe (phase-done F2 gate, --mode=both)

Phase-done F2 review gate. Scope captured to the F2 CODE diff (the `.atomic-skills/`
state churn was excluded — it is gated by `validate-state`/GATE-R2, not by adversarial
code review). Captured diff (byte-identical for local + codex): `scripts/worktree-teardown.js`
+ `tests/worktree-teardown.test.js`, 13662 bytes. Destructive signal: false (additive,
no file delete, no drop token). `--mode=both` chosen by operator (one-way-door safety module).

## Local review (same-model, sealed envelope) — 3 minor

| # | Severity | File:line | Finding | Action |
|---|---|---|---|---|
| L-1 | minor | worktree-teardown.js:86 | `!live.mergedAt` half of the not-merged guard untested | **applied** — added test (MERGED + mergedAt:null ⟹ not-merged) |
| L-2 | minor | worktree-teardown.js:79 | `!integrationRef` half of indeterminate-base untested | **applied** — added test (baseRef present + integrationRef undefined ⟹ indeterminate-base) |
| L-3 | minor | worktree-teardown.js:50-54 | `defaultGh` fail-closed stub default-path untested | **applied** — added test (default gh ⟹ gh-unauthenticated, no git call) + clarifying comment (F3 wires the real adapter) |

## Pass 1 (codex blind) — 1 major + 1 minor

### F-001 [major] correctness — worktree-teardown.js:92-101
**Claim:** squash-head-match returns `safe:true` with `branchHead===headRefOid` without
proving the local `baseRef` contains the target-side merge/squash commit.
**Confidence:** high.

### F-002 [minor] error handling — worktree-teardown.js:82
**Claim:** if `gh.prView` throws, `isTeardownSafe` throws instead of returning a decision object.
**Confidence:** medium.

## Pass 2 (codex informed) — verdict: approve, 0 findings

Reconciliation against verifiable constraints (no-production-callers; injected
fail-closed adapters; git squash creates a new SHA so local ancestry cannot prove a
squash; `gh` MERGED+baseRefName is authoritative remote integration; native `git branch -d`
second guard):

### Dropped from blind pass
- **F-001-blind [major] — DROPPED:** GitHub `MERGED` + matching `baseRefName` proves
  authoritative remote integration independent of a stale local `origin/<ref>`, and
  downstream `git branch -d` adds a native non-force deletion guard. (Confirms the
  approved Decisão 4 design: the squash path anchors on the authoritative MERGED state +
  `headRefOid`, not on local ancestry which is impossible under squash.)
- **F-002-blind [minor] — DROPPED:** the layer uses injected adapters whose default impls
  catch and fail closed, so an arbitrary throwing injected fake is outside the stated
  adapter contract.

### Maintained / Emerged
- _(none)_ / _(none)_

## Fixes applied in this session
All fixes are HARDENING (the informed pass dropped both blind findings; the local 3 were
minor coverage). Kept because they strengthen a one-way-door safety module and are
consistent with the module's established defensive style (`defaultGit` already catches its
own errors; P3 fail-safe = BLOCK).

- **worktree-teardown.js** — wrapped `gh.prView(prIdentity)` in `try/catch` ⟹ `blocked('gh-lookup-failed')`
  (defense-in-depth: the decision layer never crashes, always returns a block — even though
  Pass-2 deems a throwing adapter out-of-contract). Added a clarifying comment that the real
  `gh` adapter is wired by `project finalize` (F3) and the default is a deliberate fail-closed stub.
- **worktree-teardown.test.js** — +4 tests (21 total): gh throws ⟹ gh-lookup-failed; MERGED+mergedAt:null ⟹ not-merged;
  integrationRef absent ⟹ indeterminate-base; default gh ⟹ gh-unauthenticated (no git call).
- **Verifier after fixes:** `node --test tests/worktree-teardown.test.js` = 21 tests, 21 pass, 0 fail.

## Self-review against code-quality gates
- **G1 read-before-claim:** applied — each fix site (worktree-teardown.js:50-54, :82, :86, :79)
  was Read before editing; the diff was read, not the codex `-o` narrative.
- **G2 soft-language:** applied — fix descriptions state what the fix does (no should/probably/may).
- **G3 anti-tautology:** applied — the gh-throw test is killed by mutating the catch reason
  (`gh-lookup-failed`→`WRONG-REASON` ⟹ that test RED); the not-merged/indeterminate-base tests
  are killed by dropping their respective guard halves (mutation ⟹ wrong reason code).
- **G4 fixture realism:** N/A — pure decision logic over injected adapter results, no external data.
- **G7 anti-premature-abstraction:** applied — no new helper; the try/catch is inline at the one call site.
