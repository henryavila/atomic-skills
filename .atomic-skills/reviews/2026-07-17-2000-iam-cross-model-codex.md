| # | Severity | File:line | Claim | Impact | Recommendation | Confidence |
|---:|---|---|---|---|---|---|
| 1 | critical | src/implement-mode.js:96 | WHAT: absent `--mode` is normalized to `default`; WHY: `isAutomateActive` then treats any known `cliMode` as an explicit non-automate override at lines 186-198. The documented call shape passes `cliMode`, so `parseImplementMode(['plan']).mode` + `executionMode: automate` resolves false. | Durable stamp re-entry can silently fall back to Mode 1, allowing host product edits and skipping automate review/plan-end behavior. | Preserve absence as `undefined`/`null`, or pass `modeExplicit` into `isAutomateActive` and only let non-automate modes override the stamp when explicitly supplied. Add a test for `parseImplementMode(['plan'])` combined with `planExecutionMode: 'automate'` returning true. | high |
| 2 | critical | src/writer-lease.js:183 | WHAT: the clear “owner token” is just `planSlug + phaseId + hostId + startedAt`; WHY: those fields are stored in the readable lease file and `clearLeaseFile` only compares them at lines 486-493. | Any process/agent with repo read access can copy the token from `.atomic-skills/status/writer-leases/*.json` and clear an active lease, breaking exclusive writer protection and enabling premature clear/mode switch. | Use an unguessable nonce created by the orchestrator and keep the clearing secret outside the phase writer’s readable workspace, or store only a keyed digest in the lease. Treat current identity fields as CAS metadata, not authorization. | high |
| 3 | major | src/claim-report.js:97 | WHAT: malformed task entries are silently dropped by `map(normalizeTaskClaim).filter(Boolean)`; WHY: `normalizeTaskClaim` returns null for missing/blank `taskId` at lines 133-137, so validation never sees the bad entry. | A writer can omit or corrupt one work-order task while still returning at least one valid task; `validateClaimReport` can pass the reduced report, weakening the “one entry per task / incomplete claims do not close” gate. | Do not filter malformed task entries. Preserve them as invalid task records so `validateTaskClaim` reports `taskId is required`, and validate against the expected work-order task id set. | high |
| 4 | major | src/claim-report.js:365 | WHAT: range exclusivity only rejects identical `base+head` pairs; WHY: distinct but overlapping ranges are accepted without expansion or graph checks. | Two task claims can cover the same commits through different ranges, making destructive-diff/review pins and task ownership ambiguous before `done`. | Require disjoint expanded commit sets after merge, or require exclusive per-task `commitShas` derived from each range and reject any overlap before review/close. | medium |
| 5 | major | src/plan-end-review.js:45; meta/schemas/plan.schema.json:64 | WHAT: `planEndReviewOk` accepts `mode: 'both'`; WHY: the schema and finalize prose require `external-both` only. | Runtime gates can pass a receipt shape the plan schema rejects and that may not represent the required plan-end external-both review. | Make the helper accept only `external-both`, or normalize host-specific receipts to `external-both` only after proving they came from the plan-end external-both route. | high |
| 6 | major | src/claim-report.js:279 | WHAT: open claims only require `paths` to be an array; WHY: empty arrays pass for `claimed-pass` / `claimed-fail`. | A claim can pass validation without any touched-path evidence, reducing scope/audit checks and contradicting the contract that per-task paths are required. | For open claims, require `paths.length > 0` and non-empty trimmed string entries; reserve empty paths for `blocked`/`skipped` with notes. | high |