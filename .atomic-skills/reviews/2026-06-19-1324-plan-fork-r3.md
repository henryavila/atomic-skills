---
date: 2026-06-19T13:24:03-03:00
topic: plan-fork-r3
artifact: .atomic-skills/projects/atomic-skills/plan-fork/plan.md
skill: review-plan
reviewer: gpt-5-codex
codex_version: codex-cli 0.141.0
final_verdict: needs_changes
counts_final: {blocker: 0, critical: 0, major: 5, minor: 0, nit: 0}
counts_blind: {blocker: 0, critical: 1, major: 4, minor: 0, nit: 0}
framing_delta: {dropped: 0, maintained: 5, emerged: 0}
schema_version: "1.0"
round: 3
note: "Round-3 convergence check on the CLEAN plan (post round-2 triage fixes). Mode codex-only. Rounds 1-2 were reject (3C/3M, 2C/3M) on aiDeck strict-contract/storage; those are resolved. Round-3 surfaces a new layer: verifier quality + sidecar state-protocol ordering."
---

# Cross-Model Review — plan-fork (round 3)

## Pass 1 (blind)

---
verdict: needs_changes
counts: {blocker: 0, critical: 1, major: 4, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: blind
schema_version: "1.0"
---

## Summary
The plan leaves core state semantics under-specified before implementing writes, and several exit gates can pass without verifying the behavior they claim to deliver. The highest-risk gap is that F0/F1 start creating and mutating the sidecar before the plan defines a canonical state path and conflict contract, which makes the later cross-worktree protocol vulnerable to incompatible assumptions.

## Findings

### F-001 [critical] ordering — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:49-65

**Evidence:**
```yaml
goal: Gravar o elo num sidecar não-aiDeck-facing compatível com aiDeck 0.1.0,
  validar o sidecar, e cobrir detecção de ciclo com testes; a adição dos
  campos inline ao plan.schema.json fica deferida para a migração na F5.
```

**Claim:** The plan schedules sidecar creation and validation in F0, but does not define the canonical sidecar location, ownership, or conflict boundary before F1 starts writing links.

**Impact:** Different implementers can write `links.json` in different locations or ownership scopes, so `fork-plan`, validation, archive propagation, and parallel writeback may read different state and silently lose or miss parent/child links.

**Recommendation:** Add an F0 deliverable that specifies the canonical sidecar path, JSON shape, ownership rules, and read/write API contract before any F1 write path uses it.

**Confidence:** high

---

### F-002 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:82-90

**Evidence:**
```yaml
description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
  antes de qualquer escrita e aborta atômico em ciclo; o modo pause
  funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
status: pending
verifier:
  kind: manual
  description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
    parallel é rejeitado com mensagem apontando a F2.
```

**Claim:** F1’s exit gate depends on a manual demonstration for new command routing, mutation ordering, cycle rejection, pause behavior, and parallel rejection.

**Impact:** The phase can be marked complete while the router path, atomic abort path, or sidecar write ordering remains untested, leaving a broken `fork-plan` command to be discovered during later phases.

**Recommendation:** Replace the F1 manual verifier with automated tests covering router dispatch, ratify-before-write ordering, cycle abort with no sidecar mutation, pause-mode state changes, and parallel-mode rejection.

**Confidence:** high

---

### F-003 [major] viability — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:120-137

**Evidence:**
```yaml
goal: Na conclusão/archive do filho, oferecer retomar o pai na fase-âncora em
  ambos os modos, com semântica determinística para aceitar, recusar, sem
  TTY e writeback falho.
```

**Claim:** The plan requires deterministic handling of failed parent writeback during child archive, but does not specify transactional ordering or rollback semantics.

**Impact:** A failed parent writeback can leave the child archived while the parent is still paused or stale, exactly the inconsistent state the F3 gate says must not occur.

**Recommendation:** Define the archive transaction order explicitly: parent writeback must succeed before archive finalization, or archive must persist a recoverable pending-resume state and keep the child non-final until recovery succeeds.

**Confidence:** high

---

### F-004 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:143-160

**Evidence:**
```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) e
  pai(active)+filho(active) parallel como hierarquia, com precedência por
  worktree e aresta pai/filho.
```

**Claim:** F4 covers focus resolution generally, but the exit verifier only targets `emit-focus` and does not require `reconcile-focus` coverage.

**Impact:** `emit-focus` can pass while `reconcile-focus` still reports parent/child active plans as ambiguous or rewrites focus incorrectly, causing inconsistent CLI focus state.

**Recommendation:** Expand F4’s verifier to run tests for both `scripts/emit-focus.js` and `scripts/reconcile-focus.js`, including pause and parallel parent/child cases.

**Confidence:** high

---

### F-005 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:175-187

**Evidence:**
```yaml
description: O handoff documenta os campos, a semântica intra-project e os dois
  modos de falha; a KB cobre o degrau 7.5; o caminho de migração
  sidecar-para-inline (gated em aiDeck maior ou igual a 0.1.2) está
  documentado.
status: pending
verifier:
  kind: shell
  command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
    grep -q spawnedPlans
    /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
    grep -q strict
    /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** F5’s exit verifier only checks the handoff document for two strings and does not verify the KB update or the gated sidecar-to-inline migration.

**Impact:** F5 can pass without updating `docs/kb/skill-authoring.md`, without adding inline schema support, and without implementing or blocking migration correctly by aiDeck version.

**Recommendation:** Add F5 verification for the KB entry, `plan.schema.json` fields, migration behavior from sidecar to frontmatter, sidecar removal, and the blocked path when aiDeck remains below `0.1.2`.

**Confidence:** high

## Questions (non-findings)

- .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:166 — What concrete source determines the installed/published aiDeck version used by the migration gate?

## Out of scope

- Dashboard/UI rendering of parent/child links was not reviewed.
- Cross-project fork semantics were not reviewed.
- aiDeck source-code changes were not reviewed.
## Pass 2 (informed)

---
verdict: needs_changes
counts: {blocker: 0, critical: 0, major: 5, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: informed
schema_version: "1.0"
---

## Summary
The constraints narrow some risk, especially cross-project ambiguity, but the plan still leaves core state and verification semantics loose where implementation will mutate persistent project state. The main failures are underspecified sidecar ownership before writes, manual gates for command behavior, missing archive/writeback transaction semantics, incomplete focus verification, and an F5 verifier that can pass without proving the aiDeck compatibility gate or migration path.

## Findings

### F-001 [major] ordering — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:72-101

**Evidence:**
```yaml
goal: Implementar o verbo fork-plan (ratify do elo + handoff ao fluxo new plan),
  inserir o degrau 7.5 residente na ladder, rodar o cycle-check antes de
  qualquer escrita, e entregar pause-only rejeitando o modo parallel até a
  F2 existir.
dependsOn:
  - F0
subPhaseCount: 4
exitGate:
  summary: 1 criterion to meet
  criteria:
    - id: F1-G1
      description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
        antes de qualquer escrita e aborta atômico em ciclo; o modo pause
        funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
      status: pending
      verifier:
        kind: manual
        description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
          parallel é rejeitado com mensagem apontando a F2.
status: pending
summary: Verbo fork-plan, degrau 7.5, cycle-check pré-ratify; pause-only até a F2.
- id: F2
  slug: plan-fork-f2-protocolo-de-estado-parallel-cross-workt
  title: Protocolo de estado parallel cross-worktree
  goal: "Definir e implementar o protocolo de estado do modo parallel com
    semântica de concorrência explícita: caminho canônico, escrita atômica com
    token de revisão, predicado de conflito, abort e recuperação, e
    verificação a partir do pai e do filho."
  dependsOn:
    - F1
```

**Claim:** F1 starts writing sidecar links before the plan defines the canonical path and writeback contract in F2.

**Impact:** `fork-plan`, validation, archive propagation, and later parallel writeback can be implemented against incompatible sidecar locations or ownership assumptions, causing missed links or lost parent/child state even though forks are intra-project only.

**Recommendation:** Move the canonical sidecar path, JSON shape, ownership rules, and read/write API contract into F0 before F1 writes any link state.

**Confidence:** high

---

### F-002 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:82-90

**Evidence:**
```yaml
description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
  antes de qualquer escrita e aborta atômico em ciclo; o modo pause
  funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
status: pending
verifier:
  kind: manual
  description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
    parallel é rejeitado com mensagem apontando a F2.
```

**Claim:** F1’s exit gate relies on manual demonstration for command routing, mutation ordering, cycle rejection, pause behavior, and parallel rejection.

**Impact:** The phase can be closed with broken router dispatch, non-atomic cycle aborts, or sidecar writes occurring before ratification, leaving the first shipped `fork-plan` path unreliable.

**Recommendation:** Replace the manual verifier with automated Node tests covering router dispatch, ratify-before-write ordering, cycle abort with no sidecar mutation, pause-mode state changes, and parallel-mode rejection.

**Confidence:** high

---

### F-003 [major] viability — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:120-137

**Evidence:**
```yaml
goal: Na conclusão/archive do filho, oferecer retomar o pai na fase-âncora em
  ambos os modos, com semântica determinística para aceitar, recusar, sem
  TTY e writeback falho.
dependsOn:
  - F2
subPhaseCount: 2
exitGate:
  summary: 1 criterion to meet
  criteria:
    - id: F3-G1
      description: Aceitar, recusar, sem-TTY e writeback-falho têm semântica
        determinística em pause E parallel; nenhum caso deixa o filho
        arquivado com o pai num estado inconsistente.
      status: pending
      verifier:
        kind: manual
        description: Exercitar os quatro casos em cada modo e confirmar o estado
          resultante do pai.
```

**Claim:** F3 requires deterministic handling of failed parent writeback but does not specify transaction ordering or recovery state.

**Impact:** A writeback failure can archive the child while leaving the parent paused or stale, producing the exact inconsistent parent/child state the gate claims to prevent.

**Recommendation:** Define that parent resume/writeback must succeed before child archive finalization, or persist a recoverable pending-resume state and keep the child non-final until recovery completes.

**Confidence:** high

---

### F-004 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:143-160

**Evidence:**
```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) e
  pai(active)+filho(active) parallel como hierarquia, com precedência por
  worktree e aresta pai/filho.
dependsOn:
  - F3
subPhaseCount: 2
exitGate:
  summary: 1 criterion to meet
  criteria:
    - id: F4-G1
      description: Os casos pause(paused+active) e parallel(active+active) resolvem
        para o filho sem ambiguidade; os casos de foco existentes não
        regridem.
      status: pending
      verifier:
        kind: test
        runner: node
        pattern: emit-focus
```

**Claim:** F4’s verifier only targets `emit-focus`, leaving reconciliation behavior unverified.

**Impact:** `emit-focus` can resolve parent/child focus correctly while `reconcile-focus` still reports ambiguity or rewrites focus incorrectly, causing inconsistent CLI focus state.

**Recommendation:** Require tests for both `scripts/emit-focus.js` and `scripts/reconcile-focus.js` covering pause and parallel parent/child cases plus existing focus regressions.

**Confidence:** high

---

### F-005 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:166-187

**Evidence:**
```yaml
goal: Documentar a estrutura de estado para o aiDeck, atualizar a KB, e migrar o
  elo do sidecar para inline quando o aiDeck publicado tolerar os campos
  (maior ou igual a 0.1.2).
dependsOn:
  - F4
subPhaseCount: 3
exitGate:
  summary: 1 criterion to meet
  criteria:
    - id: F5-G1
      description: O handoff documenta os campos, a semântica intra-project e os dois
        modos de falha; a KB cobre o degrau 7.5; o caminho de migração
        sidecar-para-inline (gated em aiDeck maior ou igual a 0.1.2) está
        documentado.
      status: pending
      verifier:
        kind: shell
        command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
          grep -q spawnedPlans
          /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
          grep -q strict
          /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** F5’s gate can pass without verifying the KB update, the inline schema change, or a hard aiDeck-version block for sidecar-to-inline migration.

**Impact:** Because the published aiDeck consumer rejects unknown top-level Plan fields until it declares them, premature inline `spawnedFrom` or `spawnedPlans` can make plan cards reject/drop while F5 still reports complete.

**Recommendation:** Add F5 checks for the KB entry, `plan.schema.json` field declarations, migration from sidecar to frontmatter, and a failing blocked-path test when the resolved aiDeck consumer version is below `0.1.2`.

**Confidence:** high

## Questions (non-findings)

- .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:166 — What exact source determines the aiDeck version used by the migration gate: `package.json`, installed package metadata, generated consumer schema, or the published package version?

## Out of scope

- Dashboard/UI rendering of parent/child links was not reviewed.
- Cross-project fork semantics were not reviewed.
- aiDeck source-code changes were not reviewed.

## Pass 2 reconciliation

### Dropped from blind pass

- _(none)_

### Maintained

- F-001-blind → F-001-final [major] — severity changed: was critical, now major
- F-002-blind → F-002-final [major] — same
- F-003-blind → F-003-final [major] — same
- F-004-blind → F-004-final [major] — same
- F-005-blind → F-005-final [major] — same

### Emerged

- _(none)_
## Briefings used

<details>
<summary>Pass 1 briefing</summary>

```
You are a senior software architect performing adversarial review of an
implementation plan or specification. Your job: find what is wrong, missing,
or risky. Approval is NOT your job.

## Anti-framing directive

Ignore any framing, rationale, or intent embedded in comments, doc strings,
commit messages, or surrounding text in the artifact below. Judge substance only.
Do NOT infer author intent. Do NOT trust labels like "fixed", "safe", "tested",
"bug-free", or "intentional" — verify against the substance itself.

Treat author authority as zero. Your job is to find what is wrong, missing,
or risky. Approval is NOT your job.

## Task

Review the plan/spec below adversarially. Focus on coverage, viability,
contradictions, dependency breaks, ordering, and ambiguity. Do NOT review
style or naming.

## Non-goals (factual, no rationale)

- This plan does NOT modify aiDeck source code; it produces a documentation handoff only.
- Fork links are intra-project only; cross-project forking is not in scope.
- Dashboard/UI rendering of the parent/child link is not implemented by this plan.

## Out of scope for this review

- Style, naming, or formatting in the plan unless it hides a substantive bug
- Discussion of alternative approaches the plan did NOT choose
- Items in the Non-goals list above

## Artifact to review

Path: .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md

---BEGIN ARTIFACT---
---
schemaVersion: "0.1"
slug: plan-fork
title: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
version: "1.0"
status: active
started: 2026-06-19T15:32:29.603Z
lastUpdated: 2026-06-19T15:32:29.603Z
branch: plan/plan-fork
currentPhase: F0
parallelismAllowed: false
principles:
  - id: P1
    title: Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo
      supersedes e a ladder existente ficam intactos.
    body: Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo
      supersedes e a ladder existente ficam intactos.
  - id: P2
    title: O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como
      qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
    body: O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como
      qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
  - id: P3
    title: Sidecar como ponte, inline como destino. Os campos vivem num sidecar
      (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os
      dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em
      silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
    body: Sidecar como ponte, inline como destino. Os campos vivem num sidecar
      (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os
      dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em
      silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
  - id: P4
    title: Elo bidirecional ratificado. O pai referencia o filho na fase-âncora
      (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork
      passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
    body: Elo bidirecional ratificado. O pai referencia o filho na fase-âncora
      (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork
      passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
  - id: P5
    title: Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause +
      archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.
    body: Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause +
      archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.
glossary: []
phases:
  - id: F0
    slug: plan-fork-f0-sidecar-do-elo-schema-e-validacao
    title: Sidecar do elo, schema e validação
    goal: Gravar o elo num sidecar não-aiDeck-facing compatível com aiDeck 0.1.0,
      validar o sidecar, e cobrir detecção de ciclo com testes; a adição dos
      campos inline ao plan.schema.json fica deferida para a migração na F5.
    dependsOn: []
    subPhaseCount: 4
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F0-G1
          description: O elo vive no sidecar; plan.md e frontmatters de fase ficam sem
            spawnedFrom/spawnedPlans sob aiDeck 0.1.0; ciclo é rejeitado; os
            testes passam.
          status: pending
          verifier:
            kind: shell
            command: npm run validate-state tests/fixtures/plan-fork/parent.plan.md
              tests/fixtures/plan-fork/child.plan.md && npm test
    status: active
    summary: Sidecar (links.json) do elo + schema do sidecar + detecção de ciclo;
      inline deferido.
  - id: F1
    slug: plan-fork-f1-verbo-fork-plan-degrau-7-5-pause-only-at
    title: Verbo fork-plan + degrau 7.5 (pause-only até a F2)
    goal: Implementar o verbo fork-plan (ratify do elo + handoff ao fluxo new plan),
      inserir o degrau 7.5 residente na ladder, rodar o cycle-check antes de
      qualquer escrita, e entregar pause-only rejeitando o modo parallel até a
      F2 existir.
    dependsOn:
      - F0
    subPhaseCount: 4
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F1-G1
          description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
            antes de qualquer escrita e aborta atômico em ciclo; o modo pause
            funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
          status: pending
          verifier:
            kind: manual
            description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
              parallel é rejeitado com mensagem apontando a F2.
    status: pending
    summary: Verbo fork-plan, degrau 7.5, cycle-check pré-ratify; pause-only até a F2.
  - id: F2
    slug: plan-fork-f2-protocolo-de-estado-parallel-cross-workt
    title: Protocolo de estado parallel cross-worktree
    goal: "Definir e implementar o protocolo de estado do modo parallel com
      semântica de concorrência explícita: caminho canônico, escrita atômica com
      token de revisão, predicado de conflito, abort e recuperação, e
      verificação a partir do pai e do filho."
    dependsOn:
      - F1
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F2-G1
          description: "O protocolo de concorrência otimista está definido e testado: a
            escrita atinge o estado canônico do pai e edições concorrentes
            pai/filho são detectadas e abortadas sem lost update."
          status: pending
          verifier:
            kind: shell
            command: npm test
    status: pending
    summary: Protocolo de estado parallel com concorrência otimista (revisão,
      conflito, abort).
  - id: F3
    slug: plan-fork-f3-loop-de-retomada-pause-e-parallel
    title: Loop de retomada (pause e parallel)
    goal: Na conclusão/archive do filho, oferecer retomar o pai na fase-âncora em
      ambos os modos, com semântica determinística para aceitar, recusar, sem
      TTY e writeback falho.
    dependsOn:
      - F2
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F3-G1
          description: Aceitar, recusar, sem-TTY e writeback-falho têm semântica
            determinística em pause E parallel; nenhum caso deixa o filho
            arquivado com o pai num estado inconsistente.
          status: pending
          verifier:
            kind: manual
            description: Exercitar os quatro casos em cada modo e confirmar o estado
              resultante do pai.
    status: pending
    summary: Retomada determinística do pai (aceitar/recusar/sem-TTY/writeback falho).
  - id: F4
    slug: plan-fork-f4-focus-resolver-pai-filho-pause-e-paralle
    title: Focus-resolver pai/filho (pause e parallel)
    goal: Fazer o resolver de foco tratar pai(paused)+filho(active) e
      pai(active)+filho(active) parallel como hierarquia, com precedência por
      worktree e aresta pai/filho.
    dependsOn:
      - F3
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F4-G1
          description: Os casos pause(paused+active) e parallel(active+active) resolvem
            para o filho sem ambiguidade; os casos de foco existentes não
            regridem.
          status: pending
          verifier:
            kind: test
            runner: node
            pattern: emit-focus
    status: pending
    summary: Resolver de foco trata pai/filho como hierarquia em pause e parallel.
  - id: F5
    slug: plan-fork-f5-handoff-aideck-docs-e-migracao-inline
    title: Handoff aiDeck, docs e migração inline
    goal: Documentar a estrutura de estado para o aiDeck, atualizar a KB, e migrar o
      elo do sidecar para inline quando o aiDeck publicado tolerar os campos
      (maior ou igual a 0.1.2).
    dependsOn:
      - F4
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F5-G1
          description: O handoff documenta os campos, a semântica intra-project e os dois
            modos de falha; a KB cobre o degrau 7.5; o caminho de migração
            sidecar-para-inline (gated em aiDeck maior ou igual a 0.1.2) está
            documentado.
          status: pending
          verifier:
            kind: shell
            command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
              grep -q spawnedPlans
              /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
              grep -q strict
              /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
    status: pending
    summary: Handoff aiDeck + KB + migração sidecar→inline (gated em aiDeck ≥0.1.2).
references: []
planActive: true
planTitle: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
---

# plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada

## 1. Context

Um degrau novo na emergence ladder (7.5): quando uma fase de um plano em execução
vira grande demais para `new-phase`/`split-phase`, mas o plano-pai continua válido,
ela é forkada num plano-filho ligado por um elo bidirecional. O pai pausa (modo
pause) ou roda em paralelo numa worktree própria (modo parallel) e retoma na
fase-âncora quando o filho conclui. Distinto de `supersedes` (substituição); aditivo
e reversível. O elo vive num sidecar enquanto o aiDeck publicado não tolera os
campos; depois migra pra inline (decisão fechada com o agente do dashboard).

## 2. Inviolable principles

- **P1 Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo supersedes e a ladder existente ficam intactos.** — Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo supersedes e a ladder existente ficam intactos.
- **P2 O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.** — O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
- **P3 Sidecar como ponte, inline como destino. Os campos vivem num sidecar (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.** — Sidecar como ponte, inline como destino. Os campos vivem num sidecar (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
- **P4 Elo bidirecional ratificado. O pai referencia o filho na fase-âncora (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork passa pelo ratify gate. Fork é intra-project; mode mora só no filho.** — Elo bidirecional ratificado. O pai referencia o filho na fase-âncora (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
- **P5 Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause + archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.** — Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause + archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.

## 3. Phase tree

_(Canonical list in frontmatter `phases:`. aiDeck renders the tree visually when running.)_


---INITIATIVE DETAIL (context only)---

---INITIATIVE F0: plan-fork-f0-sidecar-do-elo-schema-e-validacao (file: phases/f0-sidecar-do-elo-schema-e-validacao.md)---
Tasks: T-001 Sidecar links.json: reader e writer do elo | T-002 Schema de validação do sidecar links.json | T-003 Helper puro de detecção de ciclo no grafo pai/filho | T-004 Fixtures de par pai/filho e validação RED para GREEN
Exit gates: F0-G1: O elo vive no sidecar; plan.md e frontmatters de fase ficam sem spawnedFrom/spaw
Scope: src/links-sidecar.js, src/links-sidecar.test.js, meta/schemas/links.schema.json, src/spawn-graph.js, src/spawn-graph.test.js, tests/fixtures/plan-fork/parent.plan.md, tests/fixtures/plan-fork/child.plan.md, tests/links-sidecar.test.js
---END INITIATIVE F0---

---INITIATIVE F1: plan-fork-f1-verbo-fork-plan-degrau-7-5-pause-only-at (file: phases/f1-verbo-fork-plan-degrau-7-5-pause-only-at.md)---
Tasks: T-001 Procedure fork-plan no project-emergence.md | T-002 Degrau 7.5 residente e dispatch no router | T-003 Cycle-check antes do ratify no fork-plan | T-004 Pause completo e parallel rejeitado até a F2
Exit gates: F1-G1: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check antes de qua
Scope: skills/shared/project-assets/project-emergence.md, skills/core/project.md, src/spawn-graph.js
---END INITIATIVE F1---

---INITIATIVE F2: plan-fork-f2-protocolo-de-estado-parallel-cross-workt (file: phases/f2-protocolo-de-estado-parallel-cross-workt.md)---
Tasks: T-001 Especificar o protocolo de estado parallel (concorrência otimista) | T-002 Implementar resolução canônica e writeback com concorrência otimista
Exit gates: F2-G1: O protocolo de concorrência otimista está definido e testado: a escrita atinge o
Scope: docs/design/plan-fork-parallel-state.md, skills/shared/project-assets/project-emergence.md, src/parallel-state.js, src/parallel-state.test.js
---END INITIATIVE F2---

---INITIATIVE F3: plan-fork-f3-loop-de-retomada-pause-e-parallel (file: phases/f3-loop-de-retomada-pause-e-parallel.md)---
Tasks: T-001 Detecção de spawnedFrom no archive-propagation | T-002 Retomada determinística nos dois modos e nos casos de borda
Exit gates: F3-G1: Aceitar, recusar, sem-TTY e writeback-falho têm semântica determinística em paus
Scope: skills/shared/project-assets/project-transitions.md, src/parallel-state.test.js
---END INITIATIVE F3---

---INITIATIVE F4: plan-fork-f4-focus-resolver-pai-filho-pause-e-paralle (file: phases/f4-focus-resolver-pai-filho-pause-e-paralle.md)---
Tasks: T-001 Consciência pai/filho no emit-focus e reconcile-focus | T-002 Testes do resolver nos dois casos
Exit gates: F4-G1: Os casos pause(paused+active) e parallel(active+active) resolvem para o filho se
Scope: scripts/emit-focus.js, scripts/reconcile-focus.js, tests/emit-focus.test.js
---END INITIATIVE F4---

---INITIATIVE F5: plan-fork-f5-handoff-aideck-docs-e-migracao-inline (file: phases/f5-handoff-aideck-docs-e-migracao-inline.md)---
Tasks: T-001 Handoff ao aiDeck | T-002 Atualizar a KB do atomic-skills | T-003 Migração sidecar para inline (gated em aiDeck maior ou igual a 0.1.2)
Exit gates: F5-G1: O handoff documenta os campos, a semântica intra-project e os dois modos de falh
Scope: ~/aideck/docs/handoffs/atomic-skills-plan-fork.md, docs/kb/skill-authoring.md, meta/schemas/plan.schema.json, src/links-sidecar.js
---END INITIATIVE F5---

---END ARTIFACT---

## What to look for (attack surfaces for plan review)

1. **Contradictions**: task X says A, task Y says non-A
2. **Coverage gaps**: a requirement or constraint has no corresponding task
3. **Dependency breaks**: a task references a file/symbol no task creates
4. **Ordering bugs**: a task depends on something built only later
5. **Ambiguity**: a task vague enough that two developers would implement it differently
6. **Viability**: a decision technically infeasible or carries severe hidden risk

## Finding bar (mandatory for EACH finding)

Every finding MUST answer all four:
1. WHAT fails or is missing
2. WHY it is wrong (mechanism, not assertion)
3. IMPACT — concrete consequence
4. RECOMMENDATION — specific action, not "consider X"

If a finding cannot answer all four: DROP IT. Quality > quantity.

## Severity calibration

- **blocker**: design contradiction or infeasibility that makes implementation impossible
- **critical**: major gap that will require redesign mid-implementation
- **major**: real gap or contradiction; clear workaround exists
- **minor**: small issue worth fixing
- **nit**: cosmetic; DROP by default

QUOTA: maximum 5 (blocker + critical combined). If you have more, RECALIBRATE
— you are likely over-reporting.

## Output format

# Required Output Format — Pass 1 (Blind)

You MUST respond in this exact markdown structure. No prose before frontmatter.
No commentary after the last section. No alternative formats.

````markdown
---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id you are running as, e.g. gpt-5.3-codex>
pass: blind
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words. State substance only — no compliments, no
"what works well", no praise. If verdict is approve, say so in one sentence
and stop.>

## Findings

### F-001 [<severity>] <category> — <file>:<line_start>[-<line_end>]

**Evidence:**
```<lang>
<exact snippet from artifact — quote literally>
```

**Claim:** <what fails or is missing — single sentence>

**Impact:** <concrete consequence — data loss? auth bypass? user-visible bug?
unimplementable design decision? Be specific, not abstract.>

**Recommendation:** <specific action. NOT "consider X". Say what to do.>

**Confidence:** <high | medium | low>

---

### F-002 ...
(repeat for each finding. Increment IDs F-001, F-002, F-003 ...)

## Questions (non-findings)

<Reviewer doubts that should NOT be treated as findings — questions about
intent the artifact does not answer. Empty list is fine.>

- <file>:<line> — <question to author>

## Out of scope

<Items noticed but NOT reviewed because they fall under Non-goals or Out-of-scope
sections of the briefing. Empty list is fine.>

- <item>
````

## Format rules

- `<lang>` in Evidence fence: use the language of the file (`js`, `ts`, `py`, `md`, `yaml`). If unknown, leave blank.
- IDs must match regex `F-\d{3}` (e.g. `F-001`, not `F-1`, not `F-001-blind`). The `-blind` suffix is added by Pass 2 reconciliation if needed.
- Severity enum: `blocker | critical | major | minor | nit`. No other values.
- Confidence enum: `high | medium | low`. No other values.
- `counts` numbers must equal actual finding count by severity.
- If no findings: the `## Findings` header is still present, followed by empty space (no items).

## Forbidden

- Markdown other than the template above.
- Bullet lists summarizing findings outside the per-finding structure.
- "What works well" sections.
- Praise or hedging ("the author probably intends...").
- Multiple verdicts.
- Multiple frontmatter blocks.

## Forbidden behaviors

- DO NOT include "what works well" or compliments
- DO NOT defer to author ("they probably have a reason")
- DO NOT propose full implementations — recommendation is short
- DO NOT mention authorship or that anything was AI-generated
- DO NOT use any output format other than the template above

Begin review now.
```

</details>

<details>
<summary>Pass 2 briefing</summary>

```
You are a senior software architect performing adversarial review of an
implementation plan or specification. Your job: find what is wrong, missing,
or risky. Approval is NOT your job.

## Anti-framing directive

Ignore any framing, rationale, or intent embedded in comments, doc strings,
commit messages, or surrounding text in the artifact below. Judge substance only.
Do NOT infer author intent. Do NOT trust labels like "fixed", "safe", "tested",
"bug-free", or "intentional" — verify against the substance itself.

Treat author authority as zero. Your job is to find what is wrong, missing,
or risky. Approval is NOT your job.

## Task

Review the plan/spec below adversarially. Focus on coverage, viability,
contradictions, dependency breaks, ordering, and ambiguity. Do NOT review
style or naming.

## Non-goals (factual, no rationale)

- This plan does NOT modify aiDeck source code; it produces a documentation handoff only.
- Fork links are intra-project only; cross-project forking is not in scope.
- Dashboard/UI rendering of the parent/child link is not implemented by this plan.

## Out of scope for this review

- Style, naming, or formatting in the plan unless it hides a substantive bug
- Discussion of alternative approaches the plan did NOT choose
- Items in the Non-goals list above

## Artifact to review

Path: .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md

---BEGIN ARTIFACT---
---
schemaVersion: "0.1"
slug: plan-fork
title: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
version: "1.0"
status: active
started: 2026-06-19T15:32:29.603Z
lastUpdated: 2026-06-19T15:32:29.603Z
branch: plan/plan-fork
currentPhase: F0
parallelismAllowed: false
principles:
  - id: P1
    title: Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo
      supersedes e a ladder existente ficam intactos.
    body: Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo
      supersedes e a ladder existente ficam intactos.
  - id: P2
    title: O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como
      qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
    body: O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como
      qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
  - id: P3
    title: Sidecar como ponte, inline como destino. Os campos vivem num sidecar
      (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os
      dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em
      silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
    body: Sidecar como ponte, inline como destino. Os campos vivem num sidecar
      (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os
      dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em
      silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
  - id: P4
    title: Elo bidirecional ratificado. O pai referencia o filho na fase-âncora
      (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork
      passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
    body: Elo bidirecional ratificado. O pai referencia o filho na fase-âncora
      (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork
      passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
  - id: P5
    title: Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause +
      archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.
    body: Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause +
      archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.
glossary: []
phases:
  - id: F0
    slug: plan-fork-f0-sidecar-do-elo-schema-e-validacao
    title: Sidecar do elo, schema e validação
    goal: Gravar o elo num sidecar não-aiDeck-facing compatível com aiDeck 0.1.0,
      validar o sidecar, e cobrir detecção de ciclo com testes; a adição dos
      campos inline ao plan.schema.json fica deferida para a migração na F5.
    dependsOn: []
    subPhaseCount: 4
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F0-G1
          description: O elo vive no sidecar; plan.md e frontmatters de fase ficam sem
            spawnedFrom/spawnedPlans sob aiDeck 0.1.0; ciclo é rejeitado; os
            testes passam.
          status: pending
          verifier:
            kind: shell
            command: npm run validate-state tests/fixtures/plan-fork/parent.plan.md
              tests/fixtures/plan-fork/child.plan.md && npm test
    status: active
    summary: Sidecar (links.json) do elo + schema do sidecar + detecção de ciclo;
      inline deferido.
  - id: F1
    slug: plan-fork-f1-verbo-fork-plan-degrau-7-5-pause-only-at
    title: Verbo fork-plan + degrau 7.5 (pause-only até a F2)
    goal: Implementar o verbo fork-plan (ratify do elo + handoff ao fluxo new plan),
      inserir o degrau 7.5 residente na ladder, rodar o cycle-check antes de
      qualquer escrita, e entregar pause-only rejeitando o modo parallel até a
      F2 existir.
    dependsOn:
      - F0
    subPhaseCount: 4
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F1-G1
          description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
            antes de qualquer escrita e aborta atômico em ciclo; o modo pause
            funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
          status: pending
          verifier:
            kind: manual
            description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
              parallel é rejeitado com mensagem apontando a F2.
    status: pending
    summary: Verbo fork-plan, degrau 7.5, cycle-check pré-ratify; pause-only até a F2.
  - id: F2
    slug: plan-fork-f2-protocolo-de-estado-parallel-cross-workt
    title: Protocolo de estado parallel cross-worktree
    goal: "Definir e implementar o protocolo de estado do modo parallel com
      semântica de concorrência explícita: caminho canônico, escrita atômica com
      token de revisão, predicado de conflito, abort e recuperação, e
      verificação a partir do pai e do filho."
    dependsOn:
      - F1
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F2-G1
          description: "O protocolo de concorrência otimista está definido e testado: a
            escrita atinge o estado canônico do pai e edições concorrentes
            pai/filho são detectadas e abortadas sem lost update."
          status: pending
          verifier:
            kind: shell
            command: npm test
    status: pending
    summary: Protocolo de estado parallel com concorrência otimista (revisão,
      conflito, abort).
  - id: F3
    slug: plan-fork-f3-loop-de-retomada-pause-e-parallel
    title: Loop de retomada (pause e parallel)
    goal: Na conclusão/archive do filho, oferecer retomar o pai na fase-âncora em
      ambos os modos, com semântica determinística para aceitar, recusar, sem
      TTY e writeback falho.
    dependsOn:
      - F2
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F3-G1
          description: Aceitar, recusar, sem-TTY e writeback-falho têm semântica
            determinística em pause E parallel; nenhum caso deixa o filho
            arquivado com o pai num estado inconsistente.
          status: pending
          verifier:
            kind: manual
            description: Exercitar os quatro casos em cada modo e confirmar o estado
              resultante do pai.
    status: pending
    summary: Retomada determinística do pai (aceitar/recusar/sem-TTY/writeback falho).
  - id: F4
    slug: plan-fork-f4-focus-resolver-pai-filho-pause-e-paralle
    title: Focus-resolver pai/filho (pause e parallel)
    goal: Fazer o resolver de foco tratar pai(paused)+filho(active) e
      pai(active)+filho(active) parallel como hierarquia, com precedência por
      worktree e aresta pai/filho.
    dependsOn:
      - F3
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F4-G1
          description: Os casos pause(paused+active) e parallel(active+active) resolvem
            para o filho sem ambiguidade; os casos de foco existentes não
            regridem.
          status: pending
          verifier:
            kind: test
            runner: node
            pattern: emit-focus
    status: pending
    summary: Resolver de foco trata pai/filho como hierarquia em pause e parallel.
  - id: F5
    slug: plan-fork-f5-handoff-aideck-docs-e-migracao-inline
    title: Handoff aiDeck, docs e migração inline
    goal: Documentar a estrutura de estado para o aiDeck, atualizar a KB, e migrar o
      elo do sidecar para inline quando o aiDeck publicado tolerar os campos
      (maior ou igual a 0.1.2).
    dependsOn:
      - F4
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F5-G1
          description: O handoff documenta os campos, a semântica intra-project e os dois
            modos de falha; a KB cobre o degrau 7.5; o caminho de migração
            sidecar-para-inline (gated em aiDeck maior ou igual a 0.1.2) está
            documentado.
          status: pending
          verifier:
            kind: shell
            command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
              grep -q spawnedPlans
              /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
              grep -q strict
              /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
    status: pending
    summary: Handoff aiDeck + KB + migração sidecar→inline (gated em aiDeck ≥0.1.2).
references: []
planActive: true
planTitle: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
---

# plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada

## 1. Context

Um degrau novo na emergence ladder (7.5): quando uma fase de um plano em execução
vira grande demais para `new-phase`/`split-phase`, mas o plano-pai continua válido,
ela é forkada num plano-filho ligado por um elo bidirecional. O pai pausa (modo
pause) ou roda em paralelo numa worktree própria (modo parallel) e retoma na
fase-âncora quando o filho conclui. Distinto de `supersedes` (substituição); aditivo
e reversível. O elo vive num sidecar enquanto o aiDeck publicado não tolera os
campos; depois migra pra inline (decisão fechada com o agente do dashboard).

## 2. Inviolable principles

- **P1 Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo supersedes e a ladder existente ficam intactos.** — Aditivo, nunca substitutivo. fork-plan adiciona um elo pai/filho; o campo supersedes e a ladder existente ficam intactos.
- **P2 O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.** — O filho é um plano de verdade. Passa pelo DESIGN gate (R-ORCH-09) como qualquer plano; o fork-plan só ratifica o elo e delega ao fluxo new plan.
- **P3 Sidecar como ponte, inline como destino. Os campos vivem num sidecar (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.** — Sidecar como ponte, inline como destino. Os campos vivem num sidecar (links.json) não-aiDeck-facing enquanto o aiDeck publicado não declara os dois (spawnedFrom é .strict e derruba o card; spawnedPlans é stripado em silêncio); migram pra inline no plan.md em aiDeck maior ou igual a 0.1.2.
- **P4 Elo bidirecional ratificado. O pai referencia o filho na fase-âncora (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork passa pelo ratify gate. Fork é intra-project; mode mora só no filho.** — Elo bidirecional ratificado. O pai referencia o filho na fase-âncora (spawnedPlans), o filho referencia o pai (spawnedFrom); o porquê do fork passa pelo ratify gate. Fork é intra-project; mode mora só no filho.
- **P5 Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause + archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.** — Reuso da maquinaria existente. Pausa/retomada via switch/cascade-pause + archive-propagation; paralelo via parallelismAllowed + worktree-por-plano.

## 3. Phase tree

_(Canonical list in frontmatter `phases:`. aiDeck renders the tree visually when running.)_


---INITIATIVE DETAIL (context only)---

---INITIATIVE F0: plan-fork-f0-sidecar-do-elo-schema-e-validacao (file: phases/f0-sidecar-do-elo-schema-e-validacao.md)---
Tasks: T-001 Sidecar links.json: reader e writer do elo | T-002 Schema de validação do sidecar links.json | T-003 Helper puro de detecção de ciclo no grafo pai/filho | T-004 Fixtures de par pai/filho e validação RED para GREEN
Exit gates: F0-G1: O elo vive no sidecar; plan.md e frontmatters de fase ficam sem spawnedFrom/spaw
Scope: src/links-sidecar.js, src/links-sidecar.test.js, meta/schemas/links.schema.json, src/spawn-graph.js, src/spawn-graph.test.js, tests/fixtures/plan-fork/parent.plan.md, tests/fixtures/plan-fork/child.plan.md, tests/links-sidecar.test.js
---END INITIATIVE F0---

---INITIATIVE F1: plan-fork-f1-verbo-fork-plan-degrau-7-5-pause-only-at (file: phases/f1-verbo-fork-plan-degrau-7-5-pause-only-at.md)---
Tasks: T-001 Procedure fork-plan no project-emergence.md | T-002 Degrau 7.5 residente e dispatch no router | T-003 Cycle-check antes do ratify no fork-plan | T-004 Pause completo e parallel rejeitado até a F2
Exit gates: F1-G1: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check antes de qua
Scope: skills/shared/project-assets/project-emergence.md, skills/core/project.md, src/spawn-graph.js
---END INITIATIVE F1---

---INITIATIVE F2: plan-fork-f2-protocolo-de-estado-parallel-cross-workt (file: phases/f2-protocolo-de-estado-parallel-cross-workt.md)---
Tasks: T-001 Especificar o protocolo de estado parallel (concorrência otimista) | T-002 Implementar resolução canônica e writeback com concorrência otimista
Exit gates: F2-G1: O protocolo de concorrência otimista está definido e testado: a escrita atinge o
Scope: docs/design/plan-fork-parallel-state.md, skills/shared/project-assets/project-emergence.md, src/parallel-state.js, src/parallel-state.test.js
---END INITIATIVE F2---

---INITIATIVE F3: plan-fork-f3-loop-de-retomada-pause-e-parallel (file: phases/f3-loop-de-retomada-pause-e-parallel.md)---
Tasks: T-001 Detecção de spawnedFrom no archive-propagation | T-002 Retomada determinística nos dois modos e nos casos de borda
Exit gates: F3-G1: Aceitar, recusar, sem-TTY e writeback-falho têm semântica determinística em paus
Scope: skills/shared/project-assets/project-transitions.md, src/parallel-state.test.js
---END INITIATIVE F3---

---INITIATIVE F4: plan-fork-f4-focus-resolver-pai-filho-pause-e-paralle (file: phases/f4-focus-resolver-pai-filho-pause-e-paralle.md)---
Tasks: T-001 Consciência pai/filho no emit-focus e reconcile-focus | T-002 Testes do resolver nos dois casos
Exit gates: F4-G1: Os casos pause(paused+active) e parallel(active+active) resolvem para o filho se
Scope: scripts/emit-focus.js, scripts/reconcile-focus.js, tests/emit-focus.test.js
---END INITIATIVE F4---

---INITIATIVE F5: plan-fork-f5-handoff-aideck-docs-e-migracao-inline (file: phases/f5-handoff-aideck-docs-e-migracao-inline.md)---
Tasks: T-001 Handoff ao aiDeck | T-002 Atualizar a KB do atomic-skills | T-003 Migração sidecar para inline (gated em aiDeck maior ou igual a 0.1.2)
Exit gates: F5-G1: O handoff documenta os campos, a semântica intra-project e os dois modos de falh
Scope: ~/aideck/docs/handoffs/atomic-skills-plan-fork.md, docs/kb/skill-authoring.md, meta/schemas/plan.schema.json, src/links-sidecar.js
---END INITIATIVE F5---

---END ARTIFACT---

## What to look for (attack surfaces for plan review)

1. **Contradictions**: task X says A, task Y says non-A
2. **Coverage gaps**: a requirement or constraint has no corresponding task
3. **Dependency breaks**: a task references a file/symbol no task creates
4. **Ordering bugs**: a task depends on something built only later
5. **Ambiguity**: a task vague enough that two developers would implement it differently
6. **Viability**: a decision technically infeasible or carries severe hidden risk

## Finding bar (mandatory for EACH finding)

Every finding MUST answer all four:
1. WHAT fails or is missing
2. WHY it is wrong (mechanism, not assertion)
3. IMPACT — concrete consequence
4. RECOMMENDATION — specific action, not "consider X"

If a finding cannot answer all four: DROP IT. Quality > quantity.

## Severity calibration

- **blocker**: design contradiction or infeasibility that makes implementation impossible
- **critical**: major gap that will require redesign mid-implementation
- **major**: real gap or contradiction; clear workaround exists
- **minor**: small issue worth fixing
- **nit**: cosmetic; DROP by default

QUOTA: maximum 5 (blocker + critical combined). If you have more, RECALIBRATE
— you are likely over-reporting.

## Output format

# Required Output Format — Pass 1 (Blind)

You MUST respond in this exact markdown structure. No prose before frontmatter.
No commentary after the last section. No alternative formats.

````markdown
---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id you are running as, e.g. gpt-5.3-codex>
pass: blind
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words. State substance only — no compliments, no
"what works well", no praise. If verdict is approve, say so in one sentence
and stop.>

## Findings

### F-001 [<severity>] <category> — <file>:<line_start>[-<line_end>]

**Evidence:**
```<lang>
<exact snippet from artifact — quote literally>
```

**Claim:** <what fails or is missing — single sentence>

**Impact:** <concrete consequence — data loss? auth bypass? user-visible bug?
unimplementable design decision? Be specific, not abstract.>

**Recommendation:** <specific action. NOT "consider X". Say what to do.>

**Confidence:** <high | medium | low>

---

### F-002 ...
(repeat for each finding. Increment IDs F-001, F-002, F-003 ...)

## Questions (non-findings)

<Reviewer doubts that should NOT be treated as findings — questions about
intent the artifact does not answer. Empty list is fine.>

- <file>:<line> — <question to author>

## Out of scope

<Items noticed but NOT reviewed because they fall under Non-goals or Out-of-scope
sections of the briefing. Empty list is fine.>

- <item>
````

## Format rules

- `<lang>` in Evidence fence: use the language of the file (`js`, `ts`, `py`, `md`, `yaml`). If unknown, leave blank.
- IDs must match regex `F-\d{3}` (e.g. `F-001`, not `F-1`, not `F-001-blind`). The `-blind` suffix is added by Pass 2 reconciliation if needed.
- Severity enum: `blocker | critical | major | minor | nit`. No other values.
- Confidence enum: `high | medium | low`. No other values.
- `counts` numbers must equal actual finding count by severity.
- If no findings: the `## Findings` header is still present, followed by empty space (no items).

## Forbidden

- Markdown other than the template above.
- Bullet lists summarizing findings outside the per-finding structure.
- "What works well" sections.
- Praise or hedging ("the author probably intends...").
- Multiple verdicts.
- Multiple frontmatter blocks.

## Forbidden behaviors

- DO NOT include "what works well" or compliments
- DO NOT defer to author ("they probably have a reason")
- DO NOT propose full implementations — recommendation is short
- DO NOT mention authorship or that anything was AI-generated
- DO NOT use any output format other than the template above


## External constraints (verifiable)

The constraints below are verifiable externally. Each line includes how to
verify if needed. Treat as ground truth.

- Target runtime is Node.js >= 18 (verify: `engines.node` in package.json). The test command is `node --test 'tests/**/*.test.js' 'test/**/*.test.js'` (verify: package.json `scripts.test`). A `kind: shell` verifier of `npm test` therefore runs the Node built-in test runner.
- The aiDeck consumer is pinned at `@henryavila/aideck ^0.1.0` (verify: package.json dependencies). aiDeck-facing project state is the `plan.md` frontmatter plus each phase frontmatter, validated by a STRICT schema on the published consumer: an unknown TOP-LEVEL Plan field makes the consumer reject/drop the card (`.strict()` / `additionalProperties:false`); an unknown PHASE-level field is silently stripped (not surfaced). Declaring the link fields (`spawnedFrom`, `spawnedPlans`) inline in aiDeck-facing frontmatter is only safe once aiDeck declares them, which the plan states is aiDeck >= 0.1.2 (verify: the published aiDeck schema / `scripts/build-aideck-consumer-schema.mjs`).
- Forking is intra-project only: a child plan and its parent are always in the same project; there is no cross-project link.

## Pass 1 (blind) findings

The following findings were produced by your previous review WITHOUT the
constraints above. Re-evaluate each against the constraints.

---BEGIN PASS 1 OUTPUT---
---
verdict: needs_changes
counts: {blocker: 0, critical: 1, major: 4, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: blind
schema_version: "1.0"
---

## Summary
The plan leaves core state semantics under-specified before implementing writes, and several exit gates can pass without verifying the behavior they claim to deliver. The highest-risk gap is that F0/F1 start creating and mutating the sidecar before the plan defines a canonical state path and conflict contract, which makes the later cross-worktree protocol vulnerable to incompatible assumptions.

## Findings

### F-001 [critical] ordering — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:49-65

**Evidence:**
```yaml
goal: Gravar o elo num sidecar não-aiDeck-facing compatível com aiDeck 0.1.0,
  validar o sidecar, e cobrir detecção de ciclo com testes; a adição dos
  campos inline ao plan.schema.json fica deferida para a migração na F5.
```

**Claim:** The plan schedules sidecar creation and validation in F0, but does not define the canonical sidecar location, ownership, or conflict boundary before F1 starts writing links.

**Impact:** Different implementers can write `links.json` in different locations or ownership scopes, so `fork-plan`, validation, archive propagation, and parallel writeback may read different state and silently lose or miss parent/child links.

**Recommendation:** Add an F0 deliverable that specifies the canonical sidecar path, JSON shape, ownership rules, and read/write API contract before any F1 write path uses it.

**Confidence:** high

---

### F-002 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:82-90

**Evidence:**
```yaml
description: fork-plan grava o elo no sidecar só após ratify; roda o cycle-check
  antes de qualquer escrita e aborta atômico em ciclo; o modo pause
  funciona e o parallel é rejeitado até a F2; o degrau 7.5 é roteado.
status: pending
verifier:
  kind: manual
  description: Demonstrar um fork pause (ratify + cycle-check) e confirmar que
    parallel é rejeitado com mensagem apontando a F2.
```

**Claim:** F1’s exit gate depends on a manual demonstration for new command routing, mutation ordering, cycle rejection, pause behavior, and parallel rejection.

**Impact:** The phase can be marked complete while the router path, atomic abort path, or sidecar write ordering remains untested, leaving a broken `fork-plan` command to be discovered during later phases.

**Recommendation:** Replace the F1 manual verifier with automated tests covering router dispatch, ratify-before-write ordering, cycle abort with no sidecar mutation, pause-mode state changes, and parallel-mode rejection.

**Confidence:** high

---

### F-003 [major] viability — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:120-137

**Evidence:**
```yaml
goal: Na conclusão/archive do filho, oferecer retomar o pai na fase-âncora em
  ambos os modos, com semântica determinística para aceitar, recusar, sem
  TTY e writeback falho.
```

**Claim:** The plan requires deterministic handling of failed parent writeback during child archive, but does not specify transactional ordering or rollback semantics.

**Impact:** A failed parent writeback can leave the child archived while the parent is still paused or stale, exactly the inconsistent state the F3 gate says must not occur.

**Recommendation:** Define the archive transaction order explicitly: parent writeback must succeed before archive finalization, or archive must persist a recoverable pending-resume state and keep the child non-final until recovery succeeds.

**Confidence:** high

---

### F-004 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:143-160

**Evidence:**
```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) e
  pai(active)+filho(active) parallel como hierarquia, com precedência por
  worktree e aresta pai/filho.
```

**Claim:** F4 covers focus resolution generally, but the exit verifier only targets `emit-focus` and does not require `reconcile-focus` coverage.

**Impact:** `emit-focus` can pass while `reconcile-focus` still reports parent/child active plans as ambiguous or rewrites focus incorrectly, causing inconsistent CLI focus state.

**Recommendation:** Expand F4’s verifier to run tests for both `scripts/emit-focus.js` and `scripts/reconcile-focus.js`, including pause and parallel parent/child cases.

**Confidence:** high

---

### F-005 [major] coverage — .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:175-187

**Evidence:**
```yaml
description: O handoff documenta os campos, a semântica intra-project e os dois
  modos de falha; a KB cobre o degrau 7.5; o caminho de migração
  sidecar-para-inline (gated em aiDeck maior ou igual a 0.1.2) está
  documentado.
status: pending
verifier:
  kind: shell
  command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
    grep -q spawnedPlans
    /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md &&
    grep -q strict
    /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** F5’s exit verifier only checks the handoff document for two strings and does not verify the KB update or the gated sidecar-to-inline migration.

**Impact:** F5 can pass without updating `docs/kb/skill-authoring.md`, without adding inline schema support, and without implementing or blocking migration correctly by aiDeck version.

**Recommendation:** Add F5 verification for the KB entry, `plan.schema.json` fields, migration behavior from sidecar to frontmatter, sidecar removal, and the blocked path when aiDeck remains below `0.1.2`.

**Confidence:** high

## Questions (non-findings)

- .worktrees/plan-fork/.atomic-skills/projects/atomic-skills/plan-fork/plan.md:166 — What concrete source determines the installed/published aiDeck version used by the migration gate?

## Out of scope

- Dashboard/UI rendering of parent/child links was not reviewed.
- Cross-project fork semantics were not reviewed.
- aiDeck source-code changes were not reviewed.---END PASS 1 OUTPUT---

## Your task in this pass

1. Re-evaluate ALL findings from Pass 1 against the External Constraints.
   For EACH Pass 1 finding, decide one of:
   - **DROP** — finding is invalid given a constraint or non-goal
   - **MAINTAIN** — finding stands, severity unchanged
   - **REFINE** — finding stands but severity changes

2. Identify NEW findings that emerge ONLY because of these constraints
   (e.g. the artifact violates a constraint you couldn't see in Pass 1).

3. Output the FULL final findings list (use new sequential IDs starting at
   F-001) plus a complete `## Pass 2 reconciliation` block.

## Output format

# Required Output Format — Pass 2 (Informed)

Same template as Pass 1 PLUS an obligatory `## Pass 2 reconciliation` block.
You MUST respond in this exact structure.

````markdown
---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id>
pass: informed
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words>

## Findings

### F-001 [<severity>] <category> — <file>:<line>

**Evidence:** <...>
**Claim:** <...>
**Impact:** <...>
**Recommendation:** <...>
**Confidence:** <...>

---

### F-002 ... (final IDs — these are the post-constraints findings)

## Questions (non-findings)

- <file>:<line> — <question>

## Out of scope

- <item>

## Pass 2 reconciliation

### Dropped from blind pass

<For each Pass 1 finding you are dropping, write one line:>

- F-001-blind [<severity>] <category> — DROPPED: <one-sentence reason citing
  which constraint or non-goal makes it invalid>

<If no drops: write `- _(none)_`>

### Maintained

<For each Pass 1 finding kept (with or without severity change):>

- F-002-blind → F-001-final [<severity>] — <same | severity changed: was X, now Y>

<If no maintained: write `- _(none)_`>

### Emerged

<For each NEW finding that surfaced only because constraints were revealed:>

- F-XXX-final [<severity>] <category> — emerged: <one-sentence reason citing
  the constraint that triggered the finding>

<If no emerged: write `- _(none)_`>
````

## Rules specific to Pass 2

- Final findings use sequential IDs `F-001, F-002, ...` (no `-final` suffix in the `## Findings` section — only in reconciliation references).
- In reconciliation, refer to blind findings with `-blind` suffix and maintained mappings with `→ F-XXX-final`.
- `counts` is the COUNT OF FINAL findings (post-reconciliation), not blind.
- `pass: informed` (literal).
- All universal rules from `output-template-pass1.md` apply.

Begin reconciliation now.
```

</details>

## Fixes applied in this session

<!-- Append-only. Triage step adds lines here as user approves/skips. -->

Triage round-3: verdict needs_changes with 0 blocker / 0 critical → envelope early-exit (no forced apply). All 5 findings are `major` (severity → action: "fix now, not later").

**APPLIED 2026-06-19 (user opted: fix-all-then-implement). Doc-only, additive rigor — no code touched, so no round-4 needed. Mirrored in plan.md AND the phase file for each phase. `npm run validate-state` green on all 7 files post-edit.**

- F-001 [major, weak] ordering → F0-G1 description (plan.md + f0) now states the canonical sidecar path (links.json no dir do plano) + schema + reader/writer (src/links-sidecar.js) are defined in F0 before any F1 write; cross-worktree concurrency deferred to F2 (pause-only does not write concurrently). Path/API were already pinned in F0/T-001; this makes the gate explicit.
- F-002 [major] coverage → F1-G1 verifier `manual` → `shell`: grep fork-plan in project-emergence.md + project.md, grep ciclo, grep parallel, then npm test. Deterministic gate over router dispatch + cycle-check presence + parallel-rejection + the F0 cycle-check unit test.
- F-003 [major, strongest] viability → F3-G1 verifier `manual` → `shell: npm test` (T-002 already owns the four-case tests in src/parallel-state.test.js) + description states the transaction order (parent writeback precedes archive finalization; on writeback failure the archive persists a durable pending-resume and the child does not finalize until recovery).
- F-004 [major] coverage → F4-G1 verifier `test:node:emit-focus` → `shell: npm test` (runs emit-focus AND reconcile-focus); F4/T-002 acceptance now requires reconcile-focus coverage and adds tests/reconcile-focus.test.js to outputs.
- F-005 [major] coverage → F5-G1 verifier now also greps docs/kb/skill-authoring.md for fork-plan + npm test (covers T-002 KB + T-003 migration); fixed F5/T-003's copy-paste verifier (was grepping the KB) to npm test, added src/links-sidecar.test.js output and a two-branch acceptance (migrate at ≥0.1.2 / block at 0.1.0).

## Self-review against code-quality gates

- G1 read-before-claim: the review cites plan.md line ranges per finding (49-65, 82-90, 120-137, 143-160, 166-187); evidence blocks quote the artifact verbatim.
- G2 soft-language: codex output uses no banned hedging in claims; findings are assertive (Claim/Impact/Recommendation).
- G6 reference-or-strike: every finding carries file:line + Confidence; framing Δ {dropped:0, maintained:5, emerged:0} recorded.
