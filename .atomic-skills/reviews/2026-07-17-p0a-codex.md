Verdict: FAIL

Counts: Critical 2, Major 4

Findings:

1. `src/recovery-cli.js:450`
Claim: `forceIncompleteUninstall()` returns `exitCode: 0` even when post-U reversal has failures and the incomplete manifest is intentionally retained.
Impact: CLI prints “Force-incomplete complete” while the install remains blocked by the same incomplete transaction. Automation will treat recovery as successful.
Recommendation: For post-U `failed.length > 0`, return non-zero, `ok: false`, and make CLI avoid the completion message.
Confidence: High

2. `src/install.js:812`
Claim: `install --repair` silently chooses user scope whenever both user and project manifests exist, even if the project manifest is the incomplete one.
Impact: Running repair from an affected project can no-op against the user install and leave the project unrecovered.
Recommendation: Detect incomplete state across both candidate scopes using `describeRecovery`; require `--project` or fail with an ambiguity message when both are possible.
Confidence: High

3. `src/uninstall.js:80`
Claim: `uninstall --force-incomplete` also defaults to user scope when both manifests exist.
Impact: A project incomplete transaction can be skipped or misreported, especially if the user install is complete, causing `refuse-complete` on the wrong base.
Recommendation: Resolve force-incomplete scope by incomplete transaction presence first, not by normal manifest precedence.
Confidence: High

4. `src/install.js:833`
Claim: `install --repair` does not perform the advertised post-U “reverse+reinstall”; it only reverses and tells the operator to rerun install.
Impact: The command leaves the system uninstalled/partially de-owned after successful repair, contrary to CLI help and recovery contract.
Recommendation: Either actually orchestrate reinstall after successful post-U reverse or rename/document the behavior as “reverse-only” and update tests accordingly.
Confidence: High

5. `src/recovery-cli.js:147`
Claim: The residual ledger is written with plain `writeFileSync`, but later logic assumes it is durable before removing `manifest.json`.
Impact: A crash or interruption can leave a truncated/missing ledger and a cleared incomplete marker, losing recovery evidence.
Recommendation: Write the ledger atomically with fsync/rename/no-follow semantics before clearing the manifest.
Confidence: Medium-High

6. `src/recovery-cli.js:409`
Claim: Invalid/unreadable incomplete manifests are treated as `{ effects: [] }`, then pre-U force recovery may unlink `manifest.json`.
Impact: The only recovery artifact can be discarded without preserving raw contents or parse failure details.
Recommendation: Quarantine/copy the raw manifest into the ledger before unlink, or refuse force recovery for unreadable JSON unless an explicit destructive flag is provided.
Confidence: Medium