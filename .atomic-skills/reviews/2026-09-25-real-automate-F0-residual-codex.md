I found **three critical, three major, and one minor** issue after a second pass. No files were changed.

1. **Critical — A lock override can supersede the real pen lock.** [automate-pen.sh:13](/Volumes/External/code/atomic-skills/.worktrees/real-automate/skills/shared/project-assets/hooks/automate-pen.sh:13), [automate-pen-hook.js:45](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/automate-pen-hook.js:45)  
   **WHAT:** Both layers choose an existing `AUTOMATE_PEN_LOCK` before the project’s `pen.lock`. **WHY:** An override containing a different `writerWorktree` controls the path decision even while the real lock exists. **Impact:** Writes to the locked project can be allowed. **Recommendation:** Give the on-disk operational lock precedence and reject conflicting overrides.

2. **Critical — Unknown tools never reach the fail-closed decision.** [automate-host-pen.js:74](/Volumes/External/code/atomic-skills/.worktrees/real-automate/src/automate-host-pen.js:74), [automate-host-pen.js:523](/Volumes/External/code/atomic-skills/.worktrees/real-automate/src/automate-host-pen.js:523)  
   **WHAT:** The registered matcher names only a fixed set of tools. **WHY:** An unlisted tool does not invoke the hook, so `decidePen`’s unknown-tool denial is unreachable for it. **Impact:** Another mutation-capable host or plugin tool can bypass the lock. **Recommendation:** Intercept every tool call while locked, then apply the allow decision inside the hook.

3. **Critical — Accepted command prefixes can change or skip hook behavior.** [automate-host-pen.js:249](/Volumes/External/code/atomic-skills/.worktrees/real-automate/src/automate-host-pen.js:249), [automate-run.js:225](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/automate-run.js:225)  
   **WHAT:** Registration accepts arbitrary `NAME=value` prefixes, while the probe extracts the script path and runs it without those assignments. **WHY:** A registered command can set `AUTOMATE_PEN_LOCK` to a forged lock or `BASH_ENV` to code that exits before the script; the probe tests neither effect. **Impact:** Startup can pass for a live hook that allows locked writes. **Recommendation:** Reject environment assignments in registered commands, or execute and validate the exact command and environment the host will use.

4. **Major — The “host write” probe does not exercise the host.** [automate-run.js:193](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/automate-run.js:193), [automate-run.js:235](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/automate-run.js:235)  
   **WHAT:** The probe reads hook JSON itself and spawns Bash directly. **WHY:** It does not establish that the host accepts the hook entry, matches the tool, or invokes the command. **Impact:** A misregistered hook can receive a passing probe while the host leaves writes unguarded. **Recommendation:** Probe through an actual host tool call, or limit the claimed result to script behavior.

5. **Major — An invented external review receipt passes.** [find-unreviewed-plans.js:76](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/find-unreviewed-plans.js:76)  
   **WHAT:** Any label other than three excluded labels, any nonempty `command` or `cli`, `exit=0`, and a pass verdict satisfy the gate. **WHY:** The parser neither identifies an external CLI nor verifies that it ran. **Impact:** `- invented: command=echo | exit=0 | verdict=PASS` clears `--require-external`; I confirmed this with a read-only function call. **Recommendation:** Validate a structured receipt tied to a recorded CLI invocation and its result.

6. **Major — Hook resolution can reject a valid `$PWD` command.** [automate-host-pen.js:273](/Volumes/External/code/atomic-skills/.worktrees/real-automate/src/automate-host-pen.js:273), [automate-run.js:238](/Volumes/External/code/atomic-skills/.worktrees/real-automate/scripts/automate-run.js:238)  
   **WHAT:** Resolution trusts inherited `env.PWD`, although the probe launches Bash with `cwd: root`. **WHY:** Bash resets `$PWD` to its actual working directory when the inherited value differs. **Impact:** With `--root` differing from inherited `PWD`, registration can reject a command Bash would resolve correctly. A read-only check returned `null` for the resolver while Bash reported the actual cwd. **Recommendation:** Resolve `$PWD` from the command’s execution cwd.

7. **Minor — `apply_patch` can be allowed without a patch.** [automate-host-pen.js:476](/Volumes/External/code/atomic-skills/.worktrees/real-automate/src/automate-host-pen.js:476)  
   **WHAT:** When `patch` is empty, path collection falls back to `filePath`; an in-tree `filePath` makes `decidePen` allow `apply_patch`. **WHY:** The patch-specific path requirement applies only to nonempty patches. **Impact:** The hook does not fail closed for an opaque patch input. I confirmed the allow result with a read-only function call. **Recommendation:** Require a nonempty, successfully parsed patch for every `apply_patch` call.