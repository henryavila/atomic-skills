I'll load the full review brief and the composite plan artifacts, then judge only substance for contradictions, gaps, and viability.Next I'll read the full design and source specs so findings rest on the actual substance, not the truncated summary.I'll cross-check the plan against live implement, phase-done, and finalize contracts for contradictions and missing wiring.Checking a few more contradiction points and line anchors for precise citations.---
verdict: needs_changes
counts: {blocker: 0, critical: 5, major: 3, minor: 0, nit: 0}
reviewer: grok-4.5
pass: blind
schema_version: "1.0"
---

## Summary

The plan still contains load-bearing contradictions on isolation (nested vs sibling), archive enforcement (hard-block vs soft pointer), and `executionMode` stamping (must vs may). Machine-checkable user validation is required by P6/finalize tasks but has no durable field, schema, or pure helper. The executable SPEC artifacts that implement will actually run (materialized F0 initiative and F1–F3 `source.json` descriptors) lag the patched `source.md`/`design.md` on those same decisions, so implementation can false-green the wrong contract.

## Findings

### F-001 [critical] contradiction — plan.md:30-34

**Evidence:**
```md
- id: P4
  title: One writer per tree per window
  body: At most one writer process on a given worktree at a time. Orchestrator
    sync-waits for the phase writer. Nested phase worktree is required when
    exclusive spawn cannot be guaranteed.
```

```md
- **Sibling isolation, never nest under the plan worktree:** the plan home is already a linked worktree (`.worktrees/<plan-slug>`). Phase isolation cuts a **sibling** worktree from the git common-dir / primary repo root … **not** a nest under `.worktrees/<plan-slug>/…`.
- **Default in v1:** prefer sibling phase worktree for the phase writer always (safer).
```

```json
"title": "Nested worktree escalation and isolation rules in implement automate path",
"acceptance": [
  "it - Automate path requires nested phase worktree when exclusive spawn and sync wait cannot be guaranteed.; …"
]
```

**Claim:** Isolation policy is simultaneously “nested required when exclusive spawn is uncertain,” “sibling always / never nest under plan worktree,” and (in executable F1 descriptor) “nested phase worktree required.”

**Impact:** Two implementers will build opposite worktree topologies; one will nest under the plan home (violating existing `worktree-isolation.md` “do not nest” and Mode 2 sibling pattern), the other will cut siblings. Merge-back, resume, and lease `worktreePath` diverge; F1 verifiers can pass greps for either phrase without a single coherent path.

**Recommendation:** Make one isolation rule canonical everywhere (plan P4, design isolation, `source.md` T-006, and `f1-…source.json` T-006): sibling worktree from git common-dir by default; forbid nest under `.worktrees/<plan-slug>/`; delete “nested phase worktree required” language or redefine it only as non-plan-home sibling isolation with one acceptance string and one verifier.

**Confidence:** high

---

### F-002 [critical] contradiction — design.md:67

**Evidence:**
```md
12. **`planEndReviewOk` is machine-enforced on finalize and archive under automate.** … Archive cannot complete for `executionMode: automate` plans without `planEndReviewOk` true. Soft “point the operator at finalize” is not enough.
```

```md
### T-011 Archive and status visibility of plan-end receipt
- acceptance: it - Archive under automate checks planEndReviewOk or points operator to finalize gate.; …
```

```json
"acceptance": [
  "it - Archive under automate checks planEndReviewOk or points operator to finalize gate.; …"
]
```

**Claim:** Archive under automate is both a hard fail without `planEndReviewOk` and allowed to soft-point the operator at finalize.

**Impact:** An implementer can satisfy T-011 by documenting a non-blocking prompt while Decision 12 / F3 goal require hard-block. Automate plans can archive without plan-end external success or recorded skip, defeating the machine gate the plan exists to add.

**Recommendation:** Change T-011 (and F3 `source.json`) to: archive under automate HARD-FAILS unless `planEndReviewOk` is true; status/drift may only surface missing receipt on read paths; remove “or points operator to finalize gate” as a terminal archive success path.

**Confidence:** high

---

### F-003 [critical] contradiction — design.md:164

**Evidence:**
```md
1. ~~executionMode stamp~~ — **CLOSED:** first successful `implement --mode=automate` entry **must** stamp `executionMode: automate` after operator confirm (interactive y). … stamp alone re-enters **review cadence + pure-maestro** on later `implement <plan>` …
```

```md
### T-009 Optional executionMode stamp on plan frontmatter
- acceptance: it - … implement may stamp executionMode automate on first automate entry when the field is chosen for durability.; …
```

```md
summary: "Política de review: phase-done always both, complex task both,
  executionMode opcional."
```

```md
No schema required for v1 if mode is session/invocation-only; optional plan frontmatter `executionMode: automate` is additive-optional …
```

**Claim:** Durable `executionMode` is mandatory for re-entry of pure-maestro + review cadence, optional/may-stamp in SPEC, still described as session-only/optional in blast-radius text, and summarized as optional on the plan phase.

**Impact:** Without a required stamp, later `implement <plan>` sessions lose automate review policy and pure-maestro unless the operator re-passes `--mode=automate`. `isAutomateActive` stamp-alone re-entry (F0/T-001, Decision 14) has no durable input; phase-done/finalize can silently fall back to non-automate DESTRUCTIVE ladder / Mode 1 coding, violating P1/P2/P5 across sessions.

**Recommendation:** Align all surfaces: first confirmed automate entry MUST stamp `executionMode: automate`; clear only via explicit clear path; rewrite T-009 title/acceptance from “may/optional durability” to “must stamp + schema accepts optional field for pre-stamp plans”; delete or rewrite blast-radius “No schema required / session-only” paragraph.

**Confidence:** high

---

### F-004 [critical] coverage — source.md:93

**Evidence:**
```md
- acceptance: it - … Finalize and archive require machine-checkable user validation of implementation and decisions after the last phase.
```

```md
- acceptance: it - … finalize requires machine-checkable user validation receipt (userValidatedAt or equivalent) of implementation and decision log after last phase and plan-end review.; …
```

```md
verifier: { kind: shell, command: "rg -n 'planEndReviewOk|external-both|skip-plan-end-review|user validat' skills/shared/project-assets/project-finalize.md && node --test tests/plan-end-review.test.js", expectExitCode: 0 }
```

**Claim:** P6 / T-010 / T-015 require a machine-checkable user-validation predicate, but no task defines field location, schema (`plan.schema.json` is `additionalProperties: false`), pure helper, unit tests, or finalize/archive wiring comparable to `planEndReviewOk`.

**Impact:** Implementers will invent incompatible stores (handoff prose, free-form plan body, unschematized frontmatter). Unschematized plan frontmatter fails `validate-state`; prose-only “user validates” greps pass without blocking archive. Finalize can close without a real operator validation receipt while claiming machine-checkable safety.

**Recommendation:** Add an F0 (or F3) pure helper + tests for `userValidationOk`/`userValidatedAt` (or equivalent), admit a durable field in schema (plan or status receipt), wire finalize/archive to hard-block when false under automate, and make exit-gate verifiers run the helper tests—not only `rg 'user validat'`.

**Confidence:** high

---

### F-005 [critical] dependency — phases/f0-foundation-mode-parse-and-pure.md:64-68

**Evidence:**
```md
acceptance:
  - it - parseImplementMode accepts --mode=automate and mode:automate tokens
    and returns mode automate.; it - absent mode or --mode=1 returns mode
    default or mode1 without treating automate as on.; it - unit tests cover
    automate, local aliases, and unknown mode rejection or ignore per chosen
    API.
```

```md
- acceptance: it - parseImplementMode accepts --mode=automate and mode:automate tokens and returns mode automate.; it - absent mode or --mode=1 returns mode default or mode1 without treating automate as on.; it - isAutomateActive implements CLI vs stamp vs clear precedence and unit tests cover the matrix including stamp-alone re-entry and clear path.; it - unknown mode is rejected with a clear error (not ignored).
```

```json
"title": "Nested worktree escalation and isolation rules in implement automate path",
"verifier": {
  "command": "rg -n 'nested phase worktree|merge-back|parallelismAllowed|exclusive' …"
}
```

```json
"title": "Optional executionMode stamp on plan frontmatter",
"verifier": {
  "command": "rg -n 'complex' skills/core/implement.md | rg -n 'both|review-code'"
}
```

**Claim:** Lazy-materialized executable SPECs that implement will run (active F0 initiative; F1–F3 `source.json`) omit or invert load-bearing acceptance that `source.md`/`design.md` claim as patched (`isAutomateActive` matrix; sibling+lease isolation; mandatory stamp; T-009 schema verifier).

**Impact:** F0 is `currentPhase: active` now. A writer following the initiative will land mode parse without stamp/clear precedence. Materializing F1 from descriptor lands nested isolation and greps that never require writer-lease tests. F2 T-009 can exit green via complex-task prose greps without touching `plan.schema.json` or `validate-state`. Patched design never reaches the tree implement executes.

**Recommendation:** Re-sync materialized F0 initiative and all F1–F3 descriptors from the canonical `source.md` (or regenerate), re-admit acceptance/verifiers, and gate materialize so descriptor-only tasks cannot lag design closures on isolation, stamp, and mode helpers.

**Confidence:** high

---

### F-006 [major] ambiguity — design.md:88-93

**Evidence:**
```md
  Step D     SYNC WAIT until writer exits; collect claim report; refuse self-certify
  Step E     for each task the agent claims finished:
               re-run verifier on orchestrator tree (verify-claim / done path)
```

```md
- **Default in v1:** prefer sibling phase worktree for the phase writer always (safer).
- **Merge-back is orchestrator git-ops only:** … Post-merge re-verify is mandatory before `done`.
```

**Claim:** Runtime spine never orders merge-from-sibling-worktree before orchestrator re-verify/`done`, while isolation defaults to a separate tree whose commits are not yet on the orchestrator tree.

**Impact:** Step E re-verifies the plan home before merge and either false-fails (missing changes) or closes tasks against a tree that never received the writer’s commits. Claim SHAs and paths touch a foreign worktree; `done` evidence anchors the wrong HEAD.

**Recommendation:** Insert an explicit Step D.5 (or expand D/E): orchestrator git-ops merge sibling → plan branch; refuse content conflict hand-edits; only then re-verify and `done` on the merged plan tree; add acceptance + verifier strings for that order in T-006/T-004.

**Confidence:** high

---

### F-007 [major] coverage — source.md:35-40

**Evidence:**
```md
### T-002 Complex-task predicate helper
- acceptance: it - … returns true when destructiveDiff flag is true.; …
- scopeBoundary: Do not call review-code or mutate initiative files.
```

```md
- acceptance: it - Under automate, before done on a complex task, orchestrator runs review-code --mode=both on the task commit range.; …
```

**Claim:** `isComplexTask` depends on a `destructiveDiff` input, but no task specifies who computes the DESTRUCTIVE signal over the task commit range, when (pre-`done`), or what durable evidence records that computation.

**Impact:** The DESTRUCTIVE leg of “complex” is either never applied (weight/tags only) or recomputed ad hoc with divergent ranges. Tasks that are complex only by deletion churn skip forced `--mode=both` before `done`, violating Decision 6 / P5 for that class.

**Recommendation:** In T-008 (or a sibling task), require orchestrator to compute DESTRUCTIVE over the task’s implementation commit range via the existing review-code heuristic, pass the boolean into `isComplexTask`, and record the flag + range in claim/close evidence before the complex review gate.

**Confidence:** high

---

### F-008 [major] ambiguity — design.md:168

**Evidence:**
```md
5. Max re-dispatch rounds for verifier/review/evaluator fail before mandatory operator stop — default **2** re-dispatches then stop (confirm in implement).
```

```md
- acceptance: it - Blocker and critical evaluation findings block phase-done until re-dispatch (max 2) or operator disposition.; …
```

```md
verifier fail ⇒ do NOT done; re-dispatch fix agent or stop for operator
if complex → review-code --mode=both on task range;
  blocker/critical → re-dispatch fix agent before done
```

**Claim:** Max re-dispatch is closed as default 2 only for evaluation findings; verifier and complex-review re-dispatch remain unbounded “or stop for operator” with no SPEC acceptance or counter.

**Impact:** Automate loops can infinite re-dispatch on verifier/review failure, or stop after different ad hoc limits per implementer. Cost and session recovery diverge; residual risk already named in plan reviews remains unadmitted.

**Recommendation:** Admit a single `maxRedispatch` (default 2) in implement maestro prose + unit/contract coverage for verifier fail, complex review blocker/critical, and evaluation blocker/critical; after N, HARD-STOP for operator with decision-log entry—no further auto re-dispatch.

**Confidence:** high

## Questions (non-findings)

- design.md:166 — When spawn degrades to “Mode-2-style sibling Codex phase worktree,” is the Codex agent still code-only (no `done`/state writes), or does it inherit Mode 2 close semantics?
- design.md:118-119 — What is the concrete host allowlist preflight that permits shared plan-tree exclusive spawn instead of sibling worktree?

## Out of scope

- Replacing Mode 1 or Mode 2 defaults for all plans
- Unattended merge / auto-merge PRs / auto-ratify lessons
- Parallel multi-phase writers in v1
- New top-level skill named `automate`
- Changing `external-both` merge semantics or adding a third external provider
- Alternative approaches not chosen by the plan (Approach B/C/D)
