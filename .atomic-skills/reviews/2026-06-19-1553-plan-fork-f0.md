# Code review — plan-fork F0 (Sidecar do elo, schema e validação)

- **Ref/scope:** `6e5a4f2..e0bdfd1` (F0 implementation diff)
- **Mode:** local (same-model sealed envelope; clean-context Agent)
- **Destructive-diff signal (G5):** false (776 insertions / 33 deletions, no `D`, no drop tokens) → local mode is correct
- **Files reviewed:** 12 (7 code: `src/links-sidecar.js`, `src/spawn-graph.js`, `meta/schemas/links.schema.json`, 4 tests/fixtures; 5 `.atomic-skills/` state)
- **Passes:** 3
- **Counts:** blocker 0, critical 0, major 1, minor 2
- **Reviewed+fixed HEAD:** `52ea43f`
- **Verifier after fix:** `npm run validate-state <fixtures> && node --test tests/links-sidecar.test.js tests/spawn-graph.test.js` → tests 40, pass 40, fail 0, exit 0

## Findings

| # | Severity | File:line | Finding | Action |
|---|----------|-----------|---------|--------|
| 1 | major | `src/links-sidecar.js:46-50` | `readLinks` did `JSON.parse(readFileSync())` with no try/catch and no shape check: an empty/truncated `links.json` threw an opaque path-less `SyntaxError`; a present-but-non-object file (`null`/array/primitive) was returned verbatim, violating the documented "or {} when absent" contract and feeding the setters (`:100-101`) a primitive they would mutate under ESM strict mode. | **FIXED** — path-bearing rethrow + non-object rejection; +3 tests. Committed `52ea43f`. |
| 2 | minor | `src/spawn-graph.js:73-98` | `hasCycle` is recursive (non-tail) and overflows the stack on a deep acyclic chain (~20k nodes → `RangeError`), whereas the sibling `reachable` (`:40`) was deliberately made iterative to terminate even on a cyclic graph. | **Recorded as lesson** — dead-but-exported (no caller in src/scripts/skills; F1 gate uses iterative `wouldCreateCycle`/`reachable`). Not fixed. |
| 3 | minor | `src/links-sidecar.js:86` | `writeLinks` is a plain non-atomic `writeFileSync` — a crash mid-write truncates the sidecar (the corrupt state finding #1 then surfaces). | **Recorded as lesson** — house convention (`manifest.js:20`, `install.js` write JSON identically; no `renameSync` in `src`). Finding #1's read-hardening already turns a truncated write into a contextual error. Not fixed. |

## Fix applied in this session

- `src/links-sidecar.js` `readLinks`: wrap `JSON.parse` in a try/catch rethrowing `links sidecar at <path> is not valid JSON: <msg>`; reject a parsed non-object with `links sidecar at <path> must be a JSON object, got <type>`. JSDoc updated to document the throw.
- `tests/links-sidecar.test.js`: new describe `read hardening` — empty file, malformed JSON, and `null`/array/number/string content each assert a path-bearing throw.

## Self-review against code-quality gates

- **G1 read-before-claim:** read `src/links-sidecar.js:46-50,96-104` and `src/spawn-graph.js:40-98` before classifying/fixing; finding lines cited verbatim.
- **G2 soft-language:** fix description and commit message scanned for the ban list — 0 occurrences (states what the fix does, not "should").
- **G3 anti-tautology:** named the mutation each new test kills — remove the try/catch → empty/malformed test RED (`SyntaxError` ≠ `/not valid JSON/` + no path); remove the non-object guard → null/array/number/string test RED (no throw → `assert.throws` fails).
- **G4 fixture realism:** N/A — the new tests construct corrupt-content sidecars in a tmp dir (the "real data" here IS the malformed bytes; no external sample applies).
- **G7 anti-premature-abstraction:** no new helper introduced; the fix is inline in `readLinks`. #3 not "fixed" to avoid diverging from the established non-atomic-write convention for one file.
