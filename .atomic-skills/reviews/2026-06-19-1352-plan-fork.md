---
date: 2026-06-19T13:52:36Z
topic: plan-fork
artifact: .atomic-skills/projects/atomic-skills/plan-fork/plan.md
skill: review-plan
reviewer: gpt-5-codex
codex_version: codex-cli 0.141.0
final_verdict: reject
counts_final: {blocker: 0, critical: 3, major: 3, minor: 0, nit: 0}
counts_blind: {blocker: 0, critical: 2, major: 3, minor: 0, nit: 0}
framing_delta: {dropped: 1, maintained: 4, emerged: 2}
schema_version: "1.0"
---

# Cross-Model Review — plan-fork

## Pass 1 (blind)

---
verdict: reject
counts: {blocker: 0, critical: 2, major: 3, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: blind
schema_version: "1.0"
---

## Summary
The plan defines a parent/child fork feature but leaves the parallel-mode state model and focus behavior underspecified. The parent and child can both remain active in separate worktrees, yet later phases only handle the paused-parent case and do not define ownership or propagation of state mutations.

Several stated requirements are not enforced by tasks or gates: DESIGN gate enforcement for the child, pre-ratify cycle prevention in `fork-plan`, and substantive verification of the aiDeck handoff. These gaps can produce invalid persisted plan graphs while still passing the plan’s stated verifiers.

## Findings

### F-001 [critical] contradiction — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-60

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) como hierarquia,
  não
```

```yaml
description: Com par forkado ativo, o foco resolve para o filho sem ambiguidade
  multi-active; os casos de foco existentes não regridem.
```

**Claim:** Parallel mode creates an active parent and active child, but the focus phase only specifies the paused-parent/active-child hierarchy, leaving the active-parent/active-child case without a resolution rule.

**Impact:** `fork-plan --mode parallel` can satisfy F1 while F3 still reports multi-active ambiguity or resolves the wrong plan, so agents may operate on the parent when the child worktree should own focus.

**Recommendation:** Add F3 tasks and tests for `parent(active)+child(active)` in parallel mode, defining focus precedence by worktree and parent/child edge, and require that case in the F3 exit gate.

**Confidence:** high

---

### F-002 [critical] viability — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-60

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
description: Arquivar um filho com spawnedFrom dispara a oferta de retomada;
  aceitar un-pausa o pai e seta currentPhase na fase-âncora.
```

**Claim:** The plan requires a child staged in its own worktree to resume or mutate the parent, but it never defines which worktree owns canonical plan state or how the child’s archive flow writes back to the parent.

**Impact:** In parallel mode, archive/resume can update a stale copy of `.atomic-skills`, fail to find the parent plan, or create divergent parent/child state across worktrees.

**Recommendation:** Add an explicit parallel-mode state protocol before F2: canonical parent path, cross-worktree lookup, write/ratify ownership, conflict handling, and verification from both parent and child worktrees.

**Confidence:** high

---

### F-003 [major] coverage gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:152-154

**Evidence:**
```markdown
- **P2 O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como** — O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
- **P3 Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho** — Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

**Claim:** The child DESIGN gate is stated as a requirement, but no phase task or exit gate requires `fork-plan` to complete that gate before ratifying `spawnedFrom`/`spawnedPlans` and activating the child.

**Impact:** A child plan can be linked into the parent graph and made active before satisfying R-ORCH-09, leaving invalid graph state that later phases treat as real.

**Recommendation:** Add F1 acceptance that `fork-plan` delegates to the existing new-plan DESIGN gate before any parent/child mutation, and that gate failure leaves both plans unchanged.

**Confidence:** high

---

### F-004 [major] ordering gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:38-40

**Evidence:**
```yaml
description: validate-state aprova um par pai/filho com
  spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
  testes novos passam.
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

**Claim:** Cycle rejection is assigned to validation, but the mutating `fork-plan` phase has no pre-ratify requirement to run the cycle check before writing the parent and child edge.

**Impact:** `fork-plan` can persist a cyclic parent/child graph that only fails on a later `validate-state`, requiring manual repair of plan files.

**Recommendation:** Add an F1 task and exit criterion requiring `fork-plan` to call the spawn graph cycle check before ratify and abort atomically before any write when a cycle is detected.

**Confidence:** medium

---

### F-005 [major] verification gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:123-130

**Evidence:**
```yaml
description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
  semântica pai/filho, a expectativa de render aninhado e o requisito
  .strict()/espelho; verificado contra o contrato publicado.
status: pending
verifier:
  kind: shell
  command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** The F4 gate promises content and contract verification, but its verifier only checks that the handoff file exists.

**Impact:** An empty or stale handoff file passes the final gate, so aiDeck can receive no usable contract for `spawnedFrom`, `spawnedPlans`, nested rendering, or `.strict()` compatibility.

**Recommendation:** Replace the F4 verifier with a script or shell check that verifies the handoff contains the required fields, parent/child semantics, nested-render expectation, and cited aiDeck contract/version evidence.

**Confidence:** high

## Questions (non-findings)

- .atomic-skills/projects/atomic-skills/plan-fork/plan.md:30 — Is `spawnedPlans[]` intentionally only child slugs, with mode and anchor only stored on the child side?

## Out of scope

- Changing `supersedes`.
- Changing existing emergence ladder steps 1-8.
- Backfilling existing plans.
- aiDeck UI or code changes.

## Pass 2 (informed)

---
verdict: reject
counts: {blocker: 0, critical: 3, major: 3, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: informed
schema_version: "1.0"
---

## Summary
The plan still has unresolved structural risk around parallel parent/child execution: it creates active parent and child states but does not define focus precedence, canonical state ownership, or cross-worktree mutation rules. The strict aiDeck consumer constraint also exposes a sequencing break: new fields are persisted before any compatible external contract is guaranteed.

The revealed constraints drop the blind DESIGN-gate finding, because the existing `new plan` flow already hard-blocks on R-ORCH-09. They add two concrete verification/compatibility defects: `npm test` does not prove `validate-state`, and the strict aiDeck mirror makes unknown field rollout unsafe without an early compatibility gate.

## Findings

### F-001 [critical] contradiction — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-110

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) como hierarquia,
  não
```

```yaml
summary: Resolver de foco trata pai-pausado + filho-ativo como hierarquia, não
  ambiguidade.
```

**Claim:** Parallel mode creates `parent(active)+child(active)`, but F3 only specifies `parent(paused)+child(active)` focus semantics.

**Impact:** `fork-plan --mode parallel` can pass F1 while focus resolution still reports multi-active ambiguity or selects the parent in the child worktree, sending subsequent agent work to the wrong plan.

**Recommendation:** Add F3 tasks and tests for `parent(active)+child(active)` in parallel mode, defining precedence by parent/child edge plus worktree context.

**Confidence:** high

---

### F-002 [critical] viability — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-87

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
description: Arquivar um filho com spawnedFrom dispara a oferta de retomada;
  aceitar un-pausa o pai e seta currentPhase na fase-âncora.
```

**Claim:** The plan requires the child archive flow to resume or mutate the parent, but never defines canonical state ownership or writeback behavior across worktrees.

**Impact:** In parallel mode, archive/resume can update a stale parent copy, fail to find the canonical parent, or leave divergent parent/child state between worktrees.

**Recommendation:** Add a pre-F2 protocol for canonical parent path resolution, cross-worktree lookup, write ownership, conflict handling, and verification from both worktrees.

**Confidence:** high

---

### F-003 [critical] dependency break — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:30-130

**Evidence:**
```yaml
goal: Adicionar `spawnedFrom` (Plano) e `phases[].spawnedPlans[]`
  (phaseDescriptor)
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
  semântica pai/filho, a expectativa de render aninhado e o requisito
  .strict()/espelho; verificado contra o contrato publicado.
```

**Claim:** The plan persists new plan/phase fields before it requires proof that the strict external aiDeck mirror accepts those fields.

**Impact:** Once F0/F1 start writing `spawnedFrom` or `spawnedPlans`, the published aiDeck strict schema can reject the entire project state on the first unknown property.

**Recommendation:** Move contract compatibility before field persistence: require either verified published aiDeck schema support or an atomic-skills serialization guard that withholds the new fields from aiDeck-facing state until support exists.

**Confidence:** high

---

### F-004 [major] ordering gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:38-60

**Evidence:**
```yaml
description: validate-state aprova um par pai/filho com
  spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
  testes novos passam.
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

**Claim:** Cycle rejection is assigned to validation, but `fork-plan` has no pre-ratify requirement to run the cycle check before writing the parent/child edge.

**Impact:** `fork-plan` can persist a cyclic graph that only fails on later validation, leaving manual repair as the recovery path.

**Recommendation:** Add an F1 task and exit criterion requiring `fork-plan` to run the spawn graph cycle check before ratification and abort before any write on failure.

**Confidence:** medium

---

### F-005 [major] verification gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:123-130

**Evidence:**
```yaml
description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
  semântica pai/filho, a expectativa de render aninhado e o requisito
  .strict()/espelho; verificado contra o contrato publicado.
status: pending
verifier:
  kind: shell
  command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** F4 promises content and contract verification, but its verifier only checks that the file exists.

**Impact:** An empty or stale handoff passes, so aiDeck receives no reliable contract for the new fields, nesting semantics, or `.strict()` mirror requirement.

**Recommendation:** Replace the verifier with a content/contract check that asserts the required sections and cites the published contract evidence.

**Confidence:** high

---

### F-006 [major] verification gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:38-44

**Evidence:**
```yaml
description: validate-state aprova um par pai/filho com
  spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
  testes novos passam.
status: pending
verifier:
  kind: shell
  command: npm test
```

**Claim:** The gate requires `validate-state`, but the verifier only runs `npm test`.

**Impact:** Schema or cross-file state failures can pass F0 if no unit test covers them, despite the gate claiming state validation is green.

**Recommendation:** Change the F0 verifier to run `npm run validate-state && npm test`.

**Confidence:** high

## Questions (non-findings)

- _(none)_

## Out of scope

- Changing `supersedes`.
- Changing existing emergence ladder steps 1-8.
- Backfilling existing plans.
- aiDeck UI or code changes.

## Pass 2 reconciliation

### Dropped from blind pass

- F-003-blind [major] coverage gap — DROPPED: the external constraint states the child is created through the existing `new plan` flow, which hard-blocks decompose without a lint-clean `design.md` under R-ORCH-09.

### Maintained

- F-001-blind → F-001-final [critical] — same
- F-002-blind → F-002-final [critical] — same
- F-004-blind → F-004-final [major] — same
- F-005-blind → F-005-final [major] — same

### Emerged

- F-003-final [critical] dependency break — emerged: the revealed `additionalProperties:false` plus aiDeck `.strict()` mirror constraint makes persisting new fields before compatibility proof unsafe.
- F-006-final [major] verification gap — emerged: the revealed command contract distinguishes `npm test` from `npm run validate-state`, so the F0 verifier does not prove its own gate.

## Briefings used

<details>
<summary>Pass 1 briefing</summary>

```
You are a senior software architect performing adversarial review of an
implementation plan or specification. Your job: find what is wrong, missing,
or risky. Approval is NOT your job.

## Anti-framing directive

Ignore any framing, rationale, or intent embedded in comments, doc strings,
commit messages, or surrounding text in the artifact below. Judge substance only.
Do NOT infer author intent. Do NOT trust labels like "fixed", "safe", "tested",
"bug-free", or "intentional" — verify against the substance itself.

Treat author authority as zero. Your job is to find what is wrong, missing,
or risky. Approval is NOT your job.

## Task

Review the plan/spec below adversarially. Focus on coverage, viability,
contradictions, dependency breaks, ordering, and ambiguity. Do NOT review
style or naming.

## Non-goals (factual, no rationale)

- Mudanças de UI ou código no aiDeck (este plano só produz um handoff documental)
- Mudanças no campo `supersedes` do schema de plano
- Mudanças nos degraus 1–8 já existentes da emergence ladder
- Backfill de planos já existentes com os campos novos

## Out of scope for this review

- Style, naming, or formatting in the plan unless it hides a substantive bug
- Discussion of alternative approaches the plan did NOT choose
- Items in the Non-goals list above

## Artifact to review

Path: .atomic-skills/projects/atomic-skills/plan-fork/plan.md

---BEGIN ARTIFACT---
---
schemaVersion: "0.1"
slug: plan-fork
title: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
version: "1.0"
status: active
started: 2026-06-19T13:21:05.162Z
lastUpdated: 2026-06-19T13:21:05.162Z
branch: plan/plan-fork
currentPhase: F0
parallelismAllowed: false
principles:
  - id: P1
    title: Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
    body: Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
  - id: P2
    title: O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
    body: O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
  - id: P3
    title: Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
    body: Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
  - id: P4
    title: Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +
    body: Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +
glossary: []
phases:
  - id: F0
    slug: plan-fork-f0-schema-validacao-do-elo
    title: Schema + validação do elo
    goal: Adicionar `spawnedFrom` (Plano) e `phases[].spawnedPlans[]`
      (phaseDescriptor)
    dependsOn: []
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F0-G1
          description: validate-state aprova um par pai/filho com
            spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
            testes novos passam.
          status: pending
          verifier:
            kind: shell
            command: npm test
    status: active
    summary: Campos spawnedFrom/spawnedPlans no schema + detecção de ciclo, com testes.
  - id: F1
    slug: plan-fork-f1-verbo-fork-plan-degrau-7-5
    title: Verbo fork-plan + degrau 7.5
    goal: Implementar o verbo `fork-plan` (ratify do elo no pai + handoff ao fluxo
    dependsOn:
      - F0
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F1-G1
          description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
            parallel mantém o pai active e estagia o filho em worktree; o degrau
            7.5 é reconhecido e roteado.
          status: pending
          verifier:
            kind: manual
            description: Demonstrar um fork em cada modo e confirmar o estado resultante
              (statuses dos planos/fases + worktree do filho).
    status: pending
    summary: Verbo fork-plan e o degrau 7.5 da ladder, cobrindo os modos pause e
      parallel.
  - id: F2
    slug: plan-fork-f2-loop-de-retomada
    title: Loop de retomada
    goal: Na conclusão/archive do plano-filho, oferecer retomar o plano-pai na
      fase-âncora
    dependsOn:
      - F1
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F2-G1
          description: Arquivar um filho com spawnedFrom dispara a oferta de retomada;
            aceitar un-pausa o pai e seta currentPhase na fase-âncora.
          status: pending
          verifier:
            kind: manual
            description: Arquivar um filho de teste e confirmar a retomada do pai na âncora
              (status active + currentPhase correto).
    status: pending
    summary: Ao concluir o filho, oferta de retomar o pai na fase-âncora.
  - id: F3
    slug: plan-fork-f3-focus-resolver-consciente-de-pai-filho
    title: Focus-resolver consciente de pai/filho
    goal: Fazer o resolver de foco tratar pai(paused)+filho(active) como hierarquia,
      não
    dependsOn:
      - F2
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F3-G1
          description: Com par forkado ativo, o foco resolve para o filho sem ambiguidade
            multi-active; os casos de foco existentes não regridem.
          status: pending
          verifier:
            kind: test
            runner: node
            pattern: emit-focus
    status: pending
    summary: Resolver de foco trata pai-pausado + filho-ativo como hierarquia, não
      ambiguidade.
  - id: F4
    slug: plan-fork-f4-handoff-aideck-docs
    title: Handoff aiDeck + docs
    goal: Documentar a estrutura de estado nova para a refatoração do aiDeck e
      atualizar a
    dependsOn:
      - F3
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F4-G1
          description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
            semântica pai/filho, a expectativa de render aninhado e o requisito
            .strict()/espelho; verificado contra o contrato publicado.
          status: pending
          verifier:
            kind: shell
            command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
    status: pending
    summary: Handoff ao aiDeck com a estrutura de estado nova + verificação do
      contrato publicado.
references: []
planActive: true
planTitle: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
---

# plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada

## 1. Context

Um degrau novo na emergence ladder (7.5): quando uma fase de um plano em execução
vira grande demais para `new-phase`/`split-phase`, mas o plano-pai continua válido,
ela é forkada num plano-filho ligado por um elo bidirecional. O pai pausa (modo
pause) ou roda em paralelo numa worktree própria (modo parallel) e retoma na
fase-âncora quando o filho conclui. Distinto de `supersedes` (substituição); aditivo
e reversível.

## 2. Inviolable principles

- **P1 Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo** — Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
- **P2 O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como** — O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
- **P3 Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho** — Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
- **P4 Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +** — Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +

## 3. Phase tree

_(Canonical list in frontmatter `phases:`. aiDeck renders the tree visually when running.)_


---INITIATIVE DETAIL (context only)---

---INITIATIVE F0: plan-fork-f0-schema-validacao-do-elo (file: phases/f0-schema-validacao-do-elo.md)---
Tasks: Campos spawnedFrom e spawnedPlans no plan.schema.json | Helper puro de detecção de ciclo no grafo pai/filho | Fixtures de par pai/filho e validação RED para GREEN
Exit gates: validate-state aprova um par pai/filho com spawnedFrom/spawnedPlans; mode inváli
Scope: not declared
---END INITIATIVE F0---
---INITIATIVE F1: plan-fork-f1-verbo-fork-plan-degrau-7-5 (file: phases/f1-verbo-fork-plan-degrau-7-5.md)---
Tasks: Procedure fork-plan no project-emergence.md | Degrau 7.5 residente e dispatch no router | Modos pause e parallel
Exit gates: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode parallel man
Scope: not declared
---END INITIATIVE F1---
---INITIATIVE F2: plan-fork-f2-loop-de-retomada (file: phases/f2-loop-de-retomada.md)---
Tasks: Detecção de spawnedFrom no archive-propagation | Aplicar a retomada do pai
Exit gates: Arquivar um filho com spawnedFrom dispara a oferta de retomada; aceitar un-pausa
Scope: not declared
---END INITIATIVE F2---
---INITIATIVE F3: plan-fork-f3-focus-resolver-consciente-de-pai-filho (file: phases/f3-focus-resolver-consciente-de-pai-filho.md)---
Tasks: Consciência pai/filho no emit-focus e reconcile-focus | Testes do resolver com par forkado
Exit gates: Com par forkado ativo, o foco resolve para o filho sem ambiguidade multi-active;
Scope: not declared
---END INITIATIVE F3---
---INITIATIVE F4: plan-fork-f4-handoff-aideck-docs (file: phases/f4-handoff-aideck-docs.md)---
Tasks: Handoff ao aiDeck | Verificar o contrato publicado do aiDeck | Atualizar a KB do atomic-skills
Exit gates: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a semântica pai/filho,
Scope: not declared
---END INITIATIVE F4---

---END ARTIFACT---

## What to look for (attack surfaces for plan review)

1. **Contradictions**: task X says A, task Y says non-A
2. **Coverage gaps**: a requirement or constraint has no corresponding task
3. **Dependency breaks**: a task references a file/symbol no task creates
4. **Ordering bugs**: a task depends on something built only later
5. **Ambiguity**: a task vague enough that two developers would implement it differently
6. **Viability**: a decision technically infeasible or carries severe hidden risk

## Finding bar (mandatory for EACH finding)

Every finding MUST answer all four:
1. WHAT fails or is missing
2. WHY it is wrong (mechanism, not assertion)
3. IMPACT — concrete consequence
4. RECOMMENDATION — specific action, not "consider X"

If a finding cannot answer all four: DROP IT. Quality > quantity.

## Severity calibration

- **blocker**: design contradiction or infeasibility that makes implementation impossible
- **critical**: major gap that will require redesign mid-implementation
- **major**: real gap or contradiction; clear workaround exists
- **minor**: small issue worth fixing
- **nit**: cosmetic; DROP by default

QUOTA: maximum 5 (blocker + critical combined). If you have more, RECALIBRATE
— you are likely over-reporting.

## Output format

---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id you are running as, e.g. gpt-5.3-codex>
pass: blind
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words. State substance only — no compliments, no
"what works well", no praise. If verdict is approve, say so in one sentence
and stop.>

## Findings

### F-001 [<severity>] <category> — <file>:<line_start>[-<line_end>]

**Evidence:**
```<lang>
<exact snippet from artifact — quote literally>
```

**Claim:** <what fails or is missing — single sentence>

**Impact:** <concrete consequence — data loss? auth bypass? user-visible bug?
unimplementable design decision? Be specific, not abstract.>

**Recommendation:** <specific action. NOT "consider X". Say what to do.>

**Confidence:** <high | medium | low>

---

### F-002 ...
(repeat for each finding. Increment IDs F-001, F-002, F-003 ...)

## Questions (non-findings)

<Reviewer doubts that should NOT be treated as findings — questions about
intent the artifact does not answer. Empty list is fine.>

- <file>:<line> — <question to author>

## Out of scope

<Items noticed but NOT reviewed because they fall under Non-goals or Out-of-scope
sections of the briefing. Empty list is fine.>

- <item>

## Forbidden behaviors

- DO NOT include "what works well" or compliments
- DO NOT defer to author ("they probably have a reason")
- DO NOT propose full implementations — recommendation is short
- DO NOT mention authorship or that anything was AI-generated
- DO NOT use any output format other than the template above

Begin review now.
```

</details>

<details>
<summary>Pass 2 briefing</summary>

```
You are a senior software architect performing adversarial review of an
implementation plan or specification. Your job: find what is wrong, missing,
or risky. Approval is NOT your job.

## Anti-framing directive

Ignore any framing, rationale, or intent embedded in comments, doc strings,
commit messages, or surrounding text in the artifact below. Judge substance only.
Do NOT infer author intent. Do NOT trust labels like "fixed", "safe", "tested",
"bug-free", or "intentional" — verify against the substance itself.

Treat author authority as zero. Your job is to find what is wrong, missing,
or risky. Approval is NOT your job.

## Task

Review the plan/spec below adversarially. Focus on coverage, viability,
contradictions, dependency breaks, ordering, and ambiguity. Do NOT review
style or naming.

## Non-goals (factual, no rationale)

- Mudanças de UI ou código no aiDeck (este plano só produz um handoff documental)
- Mudanças no campo `supersedes` do schema de plano
- Mudanças nos degraus 1–8 já existentes da emergence ladder
- Backfill de planos já existentes com os campos novos

## Out of scope for this review

- Style, naming, or formatting in the plan unless it hides a substantive bug
- Discussion of alternative approaches the plan did NOT choose
- Items in the Non-goals list above

## Artifact to review

Path: .atomic-skills/projects/atomic-skills/plan-fork/plan.md

---BEGIN ARTIFACT---
---
schemaVersion: "0.1"
slug: plan-fork
title: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
version: "1.0"
status: active
started: 2026-06-19T13:21:05.162Z
lastUpdated: 2026-06-19T13:21:05.162Z
branch: plan/plan-fork
currentPhase: F0
parallelismAllowed: false
principles:
  - id: P1
    title: Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
    body: Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
  - id: P2
    title: O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
    body: O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
  - id: P3
    title: Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
    body: Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
  - id: P4
    title: Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +
    body: Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +
glossary: []
phases:
  - id: F0
    slug: plan-fork-f0-schema-validacao-do-elo
    title: Schema + validação do elo
    goal: Adicionar `spawnedFrom` (Plano) e `phases[].spawnedPlans[]`
      (phaseDescriptor)
    dependsOn: []
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F0-G1
          description: validate-state aprova um par pai/filho com
            spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
            testes novos passam.
          status: pending
          verifier:
            kind: shell
            command: npm test
    status: active
    summary: Campos spawnedFrom/spawnedPlans no schema + detecção de ciclo, com testes.
  - id: F1
    slug: plan-fork-f1-verbo-fork-plan-degrau-7-5
    title: Verbo fork-plan + degrau 7.5
    goal: Implementar o verbo `fork-plan` (ratify do elo no pai + handoff ao fluxo
    dependsOn:
      - F0
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F1-G1
          description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
            parallel mantém o pai active e estagia o filho em worktree; o degrau
            7.5 é reconhecido e roteado.
          status: pending
          verifier:
            kind: manual
            description: Demonstrar um fork em cada modo e confirmar o estado resultante
              (statuses dos planos/fases + worktree do filho).
    status: pending
    summary: Verbo fork-plan e o degrau 7.5 da ladder, cobrindo os modos pause e
      parallel.
  - id: F2
    slug: plan-fork-f2-loop-de-retomada
    title: Loop de retomada
    goal: Na conclusão/archive do plano-filho, oferecer retomar o plano-pai na
      fase-âncora
    dependsOn:
      - F1
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F2-G1
          description: Arquivar um filho com spawnedFrom dispara a oferta de retomada;
            aceitar un-pausa o pai e seta currentPhase na fase-âncora.
          status: pending
          verifier:
            kind: manual
            description: Arquivar um filho de teste e confirmar a retomada do pai na âncora
              (status active + currentPhase correto).
    status: pending
    summary: Ao concluir o filho, oferta de retomar o pai na fase-âncora.
  - id: F3
    slug: plan-fork-f3-focus-resolver-consciente-de-pai-filho
    title: Focus-resolver consciente de pai/filho
    goal: Fazer o resolver de foco tratar pai(paused)+filho(active) como hierarquia,
      não
    dependsOn:
      - F2
    subPhaseCount: 2
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F3-G1
          description: Com par forkado ativo, o foco resolve para o filho sem ambiguidade
            multi-active; os casos de foco existentes não regridem.
          status: pending
          verifier:
            kind: test
            runner: node
            pattern: emit-focus
    status: pending
    summary: Resolver de foco trata pai-pausado + filho-ativo como hierarquia, não
      ambiguidade.
  - id: F4
    slug: plan-fork-f4-handoff-aideck-docs
    title: Handoff aiDeck + docs
    goal: Documentar a estrutura de estado nova para a refatoração do aiDeck e
      atualizar a
    dependsOn:
      - F3
    subPhaseCount: 3
    exitGate:
      summary: 1 criterion to meet
      criteria:
        - id: F4-G1
          description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
            semântica pai/filho, a expectativa de render aninhado e o requisito
            .strict()/espelho; verificado contra o contrato publicado.
          status: pending
          verifier:
            kind: shell
            command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
    status: pending
    summary: Handoff ao aiDeck com a estrutura de estado nova + verificação do
      contrato publicado.
references: []
planActive: true
planTitle: plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada
---

# plan-fork — fases que viram planos-filho, com pausa/paralelo e retomada

## 1. Context

Um degrau novo na emergence ladder (7.5): quando uma fase de um plano em execução
vira grande demais para `new-phase`/`split-phase`, mas o plano-pai continua válido,
ela é forkada num plano-filho ligado por um elo bidirecional. O pai pausa (modo
pause) ou roda em paralelo numa worktree própria (modo parallel) e retoma na
fase-âncora quando o filho conclui. Distinto de `supersedes` (substituição); aditivo
e reversível.

## 2. Inviolable principles

- **P1 Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo** — Aditivo, nunca substitutivo. `fork-plan` adiciona um elo pai/filho; o campo
- **P2 O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como** — O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
- **P3 Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho** — Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
- **P4 Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +** — Reuso da maquinaria existente. Pausa/retomada via `switch`/cascade-pause +

## 3. Phase tree

_(Canonical list in frontmatter `phases:`. aiDeck renders the tree visually when running.)_


---INITIATIVE DETAIL (context only)---

---INITIATIVE F0: plan-fork-f0-schema-validacao-do-elo (file: phases/f0-schema-validacao-do-elo.md)---
Tasks: Campos spawnedFrom e spawnedPlans no plan.schema.json | Helper puro de detecção de ciclo no grafo pai/filho | Fixtures de par pai/filho e validação RED para GREEN
Exit gates: validate-state aprova um par pai/filho com spawnedFrom/spawnedPlans; mode inváli
Scope: not declared
---END INITIATIVE F0---
---INITIATIVE F1: plan-fork-f1-verbo-fork-plan-degrau-7-5 (file: phases/f1-verbo-fork-plan-degrau-7-5.md)---
Tasks: Procedure fork-plan no project-emergence.md | Degrau 7.5 residente e dispatch no router | Modos pause e parallel
Exit gates: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode parallel man
Scope: not declared
---END INITIATIVE F1---
---INITIATIVE F2: plan-fork-f2-loop-de-retomada (file: phases/f2-loop-de-retomada.md)---
Tasks: Detecção de spawnedFrom no archive-propagation | Aplicar a retomada do pai
Exit gates: Arquivar um filho com spawnedFrom dispara a oferta de retomada; aceitar un-pausa
Scope: not declared
---END INITIATIVE F2---
---INITIATIVE F3: plan-fork-f3-focus-resolver-consciente-de-pai-filho (file: phases/f3-focus-resolver-consciente-de-pai-filho.md)---
Tasks: Consciência pai/filho no emit-focus e reconcile-focus | Testes do resolver com par forkado
Exit gates: Com par forkado ativo, o foco resolve para o filho sem ambiguidade multi-active;
Scope: not declared
---END INITIATIVE F3---
---INITIATIVE F4: plan-fork-f4-handoff-aideck-docs (file: phases/f4-handoff-aideck-docs.md)---
Tasks: Handoff ao aiDeck | Verificar o contrato publicado do aiDeck | Atualizar a KB do atomic-skills
Exit gates: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a semântica pai/filho,
Scope: not declared
---END INITIATIVE F4---

---END ARTIFACT---

## What to look for (attack surfaces for plan review)

1. **Contradictions**: task X says A, task Y says non-A
2. **Coverage gaps**: a requirement or constraint has no corresponding task
3. **Dependency breaks**: a task references a file/symbol no task creates
4. **Ordering bugs**: a task depends on something built only later
5. **Ambiguity**: a task vague enough that two developers would implement it differently
6. **Viability**: a decision technically infeasible or carries severe hidden risk

## Finding bar (mandatory for EACH finding)

Every finding MUST answer all four:
1. WHAT fails or is missing
2. WHY it is wrong (mechanism, not assertion)
3. IMPACT — concrete consequence
4. RECOMMENDATION — specific action, not "consider X"

If a finding cannot answer all four: DROP IT. Quality > quantity.

## Severity calibration

- **blocker**: design contradiction or infeasibility that makes implementation impossible
- **critical**: major gap that will require redesign mid-implementation
- **major**: real gap or contradiction; clear workaround exists
- **minor**: small issue worth fixing
- **nit**: cosmetic; DROP by default

QUOTA: maximum 5 (blocker + critical combined). If you have more, RECALIBRATE
— you are likely over-reporting.

## Output format

---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id you are running as, e.g. gpt-5.3-codex>
pass: blind
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words. State substance only — no compliments, no
"what works well", no praise. If verdict is approve, say so in one sentence
and stop.>

## Findings

### F-001 [<severity>] <category> — <file>:<line_start>[-<line_end>]

**Evidence:**
```<lang>
<exact snippet from artifact — quote literally>
```

**Claim:** <what fails or is missing — single sentence>

**Impact:** <concrete consequence — data loss? auth bypass? user-visible bug?
unimplementable design decision? Be specific, not abstract.>

**Recommendation:** <specific action. NOT "consider X". Say what to do.>

**Confidence:** <high | medium | low>

---

### F-002 ...
(repeat for each finding. Increment IDs F-001, F-002, F-003 ...)

## Questions (non-findings)

<Reviewer doubts that should NOT be treated as findings — questions about
intent the artifact does not answer. Empty list is fine.>

- <file>:<line> — <question to author>

## Out of scope

<Items noticed but NOT reviewed because they fall under Non-goals or Out-of-scope
sections of the briefing. Empty list is fine.>

- <item>

## Forbidden behaviors

- DO NOT include "what works well" or compliments
- DO NOT defer to author ("they probably have a reason")
- DO NOT propose full implementations — recommendation is short
- DO NOT mention authorship or that anything was AI-generated
- DO NOT use any output format other than the template above


## External constraints (verifiable)

The constraints below are verifiable externally. Each line includes how to
verify if needed. Treat as ground truth.

- `meta/schemas/plan.schema.json` declara `"additionalProperties": false` e sua description afirma espelhar `aideck/src/schemas/project-status.ts:Plan` (verify: head do arquivo).
- Exit-gate `status` é enum `pending|met|deferred`; o aiDeck valida o estado do projeto com schema `.strict()` e rejeita o projeto inteiro na primeira violação (verify: skills/core/project.md, invariante de gate-status).
- Campos novos de plano/fase precisam ser aditivos e opcionais — o schema é consumido por um aiDeck publicado externo (verify: meta/schemas/plan.schema.json).
- Node engine `>=18`; testes via `npm test` (node test runner); estado validado via `npm run validate-state` (verify: package.json + CLAUDE.md).
- A emergence ladder é residente em `skills/core/project.md`; procedures por degrau em `skills/shared/project-assets/project-emergence.md` (verify: esses arquivos).
- Pontos de reuso já existem: `switch`/cascade-pause e archive-propagation em `skills/shared/project-assets/project-transitions.md`; flag `parallelismAllowed` no schema; o enforcer worktree-por-plano é o objeto do plano `multiplan-focus-resolution` (verify: esses arquivos).
- O plano-filho é criado pelo fluxo `new plan` existente, que HARD-BLOCKA o decompose sem um `design.md` lint-clean (R-ORCH-09) (verify: project-create-plan.md Stage 4).

## Pass 1 (blind) findings

The following findings were produced by your previous review WITHOUT the
constraints above. Re-evaluate each against the constraints.

---BEGIN PASS 1 OUTPUT---
---
verdict: reject
counts: {blocker: 0, critical: 2, major: 3, minor: 0, nit: 0}
reviewer: gpt-5-codex
pass: blind
schema_version: "1.0"
---

## Summary
The plan defines a parent/child fork feature but leaves the parallel-mode state model and focus behavior underspecified. The parent and child can both remain active in separate worktrees, yet later phases only handle the paused-parent case and do not define ownership or propagation of state mutations.

Several stated requirements are not enforced by tasks or gates: DESIGN gate enforcement for the child, pre-ratify cycle prevention in `fork-plan`, and substantive verification of the aiDeck handoff. These gaps can produce invalid persisted plan graphs while still passing the plan’s stated verifiers.

## Findings

### F-001 [critical] contradiction — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-60

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
goal: Fazer o resolver de foco tratar pai(paused)+filho(active) como hierarquia,
  não
```

```yaml
description: Com par forkado ativo, o foco resolve para o filho sem ambiguidade
  multi-active; os casos de foco existentes não regridem.
```

**Claim:** Parallel mode creates an active parent and active child, but the focus phase only specifies the paused-parent/active-child hierarchy, leaving the active-parent/active-child case without a resolution rule.

**Impact:** `fork-plan --mode parallel` can satisfy F1 while F3 still reports multi-active ambiguity or resolves the wrong plan, so agents may operate on the parent when the child worktree should own focus.

**Recommendation:** Add F3 tasks and tests for `parent(active)+child(active)` in parallel mode, defining focus precedence by worktree and parent/child edge, and require that case in the F3 exit gate.

**Confidence:** high

---

### F-002 [critical] viability — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:58-60

**Evidence:**
```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

```yaml
description: Arquivar um filho com spawnedFrom dispara a oferta de retomada;
  aceitar un-pausa o pai e seta currentPhase na fase-âncora.
```

**Claim:** The plan requires a child staged in its own worktree to resume or mutate the parent, but it never defines which worktree owns canonical plan state or how the child’s archive flow writes back to the parent.

**Impact:** In parallel mode, archive/resume can update a stale copy of `.atomic-skills`, fail to find the parent plan, or create divergent parent/child state across worktrees.

**Recommendation:** Add an explicit parallel-mode state protocol before F2: canonical parent path, cross-worktree lookup, write/ratify ownership, conflict handling, and verification from both parent and child worktrees.

**Confidence:** high

---

### F-003 [major] coverage gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:152-154

**Evidence:**
```markdown
- **P2 O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como** — O filho é um plano de verdade. Ele passa pelo DESIGN gate (R-ORCH-09) como
- **P3 Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho** — Elo bidirecional ratificado. O pai grava `spawnedPlans[]` na fase, o filho
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

**Claim:** The child DESIGN gate is stated as a requirement, but no phase task or exit gate requires `fork-plan` to complete that gate before ratifying `spawnedFrom`/`spawnedPlans` and activating the child.

**Impact:** A child plan can be linked into the parent graph and made active before satisfying R-ORCH-09, leaving invalid graph state that later phases treat as real.

**Recommendation:** Add F1 acceptance that `fork-plan` delegates to the existing new-plan DESIGN gate before any parent/child mutation, and that gate failure leaves both plans unchanged.

**Confidence:** high

---

### F-004 [major] ordering gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:38-40

**Evidence:**
```yaml
description: validate-state aprova um par pai/filho com
  spawnedFrom/spawnedPlans; mode inválido e ciclo são rejeitados; os
  testes novos passam.
```

```yaml
description: fork-plan --mode pause pausa pai+fase e cria o filho active; --mode
  parallel mantém o pai active e estagia o filho em worktree; o degrau
  7.5 é reconhecido e roteado.
```

**Claim:** Cycle rejection is assigned to validation, but the mutating `fork-plan` phase has no pre-ratify requirement to run the cycle check before writing the parent and child edge.

**Impact:** `fork-plan` can persist a cyclic parent/child graph that only fails on a later `validate-state`, requiring manual repair of plan files.

**Recommendation:** Add an F1 task and exit criterion requiring `fork-plan` to call the spawn graph cycle check before ratify and abort atomically before any write when a cycle is detected.

**Confidence:** medium

---

### F-005 [major] verification gap — .atomic-skills/projects/atomic-skills/plan-fork/plan.md:123-130

**Evidence:**
```yaml
description: O handoff em ~/aideck documenta spawnedFrom/spawnedPlans, a
  semântica pai/filho, a expectativa de render aninhado e o requisito
  .strict()/espelho; verificado contra o contrato publicado.
status: pending
verifier:
  kind: shell
  command: test -f /home/henry/aideck/docs/handoffs/atomic-skills-plan-fork.md
```

**Claim:** The F4 gate promises content and contract verification, but its verifier only checks that the handoff file exists.

**Impact:** An empty or stale handoff file passes the final gate, so aiDeck can receive no usable contract for `spawnedFrom`, `spawnedPlans`, nested rendering, or `.strict()` compatibility.

**Recommendation:** Replace the F4 verifier with a script or shell check that verifies the handoff contains the required fields, parent/child semantics, nested-render expectation, and cited aiDeck contract/version evidence.

**Confidence:** high

## Questions (non-findings)

- .atomic-skills/projects/atomic-skills/plan-fork/plan.md:30 — Is `spawnedPlans[]` intentionally only child slugs, with mode and anchor only stored on the child side?

## Out of scope

- Changing `supersedes`.
- Changing existing emergence ladder steps 1-8.
- Backfilling existing plans.
- aiDeck UI or code changes.
---END PASS 1 OUTPUT---

## Your task in this pass

1. Re-evaluate ALL findings from Pass 1 against the External Constraints.
   For EACH Pass 1 finding, decide one of:
   - **DROP** — finding is invalid given a constraint or non-goal
   - **MAINTAIN** — finding stands, severity unchanged
   - **REFINE** — finding stands but severity changes

2. Identify NEW findings that emerge ONLY because of these constraints
   (e.g. the artifact violates a constraint you couldn't see in Pass 1).

3. Output the FULL final findings list (use new sequential IDs starting at
   F-001) plus a complete `## Pass 2 reconciliation` block.

## Output format

---
verdict: <approve | approve_with_nits | needs_changes | reject>
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
reviewer: <model id>
pass: informed
schema_version: "1.0"
---

## Summary
<1-2 paragraphs, max 200 words>

## Findings

### F-001 [<severity>] <category> — <file>:<line>

**Evidence:** <...>
**Claim:** <...>
**Impact:** <...>
**Recommendation:** <...>
**Confidence:** <...>

---

### F-002 ... (final IDs — these are the post-constraints findings)

## Questions (non-findings)

- <file>:<line> — <question>

## Out of scope

- <item>

## Pass 2 reconciliation

### Dropped from blind pass

<For each Pass 1 finding you are dropping, write one line:>

- F-001-blind [<severity>] <category> — DROPPED: <one-sentence reason citing
  which constraint or non-goal makes it invalid>

<If no drops: write `- _(none)_`>

### Maintained

<For each Pass 1 finding kept (with or without severity change):>

- F-002-blind → F-001-final [<severity>] — <same | severity changed: was X, now Y>

<If no maintained: write `- _(none)_`>

### Emerged

<For each NEW finding that surfaced only because constraints were revealed:>

- F-XXX-final [<severity>] <category> — emerged: <one-sentence reason citing
  the constraint that triggered the finding>

<If no emerged: write `- _(none)_`>

Begin reconciliation now.
```

</details>

## Fixes applied in this session

<!-- Append-only. Triagem step adds lines here as user approves/skips. -->

Triage 2026-06-19 (user dispositions): re-decompose from corrected source, 5→6 phases.

- **F-001 + F-002 [critical] — parallel mode underspecified:** APPLIED via a dedicated new phase **F2 — Protocolo de estado parallel cross-worktree** (canonical-path resolution, cross-worktree writeback, conflict handling, verification from both worktrees). F3 (resume) and F4 (focus) now consume it; **F4 explicitly covers the `parent(active)+child(active)` focus case** (closes F-001). User chose "fase dedicada".
- **F-003 [critical] — persist-before-compat ordering:** APPLIED. Compat-gate is now the FIRST task of F0 (`T-001` aiDeck serialization guard that omits `spawnedFrom`/`spawnedPlans` from aiDeck-facing state until the published `.strict()` consumer tolerates them). New principle **P3 "Compat antes de emitir"**. The old F4 "verify published contract" task folded into this front gate.
- **F-004 [major] — cycle-check pre-ratify:** APPLIED. New `F1/T-003` requires `fork-plan` to run the cycle-check (F0 helper) before ratify and abort atomically on a cycle; recorded in `F1-G1`.
- **F-005 [major] — F4 handoff verifier only checked existence:** APPLIED. `F5-G1` + `F5/T-001` verifier now greps the handoff content (`spawnedFrom`, `strict`), not just `test -f`.
- **F-006 [major] — F0 verifier ran only `npm test`:** APPLIED. `F0-G1` verifier is now `npm run validate-state … && npm test`.

Post-fix state: 7/7 files schema-valid; `unknownDeps: []`; 0 summary/signalless gaps; G2 = 0.
Dropped finding F-003-blind (DESIGN gate) needed no action — the `new plan` flow already enforces R-ORCH-09 (principle P2).
